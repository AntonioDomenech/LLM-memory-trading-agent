# AAPL SEC/Gemma lean science v3.16 preregistration

## Status and decision boundary

This document preregisters V3.16 before any V3.16 implementation commit,
qualification, one-shot preflight reservation, source replay, SEC request,
Yahoo request, Ollama request, Gemma generation, market-value read, prediction,
action, return, performance calculation, confirmation or live-style data open,
paid API, broker action, or real-money action.

V3.16 is a process-only successor to the consumed zero-effect V3.15 preflight
rejection. It changes no scientific hypothesis, source row, filing byte,
nullable-source rule, prompt, model, feature, market series, learner, policy,
threshold, transaction cost, score, gate, stage boundary, or effect budget.
Its only production correction is the exact isolated-qualification bootstrap
addition below. After the original cleared 16-name launch environment is
authenticated, the bootstrap installs a controlled six-name Windows recovery
envelope for pytest only. It adds one direct regression that launches the two
unchanged shared recovery nodes under that exact bootstrap and environment.

V3.15 collected and executed all 324 Phase 1 nodes successfully with exit zero,
zero failures, zero errors, zero skips, exact collected/JUnit node order, and
the exact ordered-node SHA-256. Its corrected summary parser accepted the
valid long-duration receipt. Phase 2 then collected exactly 655 nodes with the
frozen ordered hash and executed all 655; 653 passed and exactly two failed.
Both failures called `_publication_recovery_host_environment()` and stopped at
the first absent name, `USERPROFILE`, because the authenticated 16-name child
environment intentionally omitted the Windows recovery envelope. JUnit counts,
terminal-summary counts, collection/execution order, and node hashes all
matched; this was not a receipt-parser or node-order failure. Phase 3, source
replay, and every SEC, Yahoo, Ollama, Gemma, market, performance, paid, broker,
and real-money effect remained unopened. V3.15 is permanently rejected and may
not be retried, resumed, rewritten, force-replaced, or reinterpreted as a
strategy result.

## Immutable base and historical authority

The V3.16 branch is
`codex/aapl-sec-gemma-lean-science-v3-16`. Its immutable successor base is the
pushed V3.15 comparison-only rejection commit
`7e124c309356fbba8c43868fd46c23ceb09137dd`, tree
`6e1822bcb509d0abd318404f3c81e415a3a1a12c`, whose sole parent is pushed
`F315` commit `776e5149e1cef0ebbbc50b78653b97be4f5df0ef`, tree
`4f49c1e2402674c193f24b1328c31b8ce6278805`.

The V3.15 public failure artifact is
`e/aapl_sec_gemma_lean_science_v3_15/DEVELOPMENT_PREFLIGHT.json`, Git blob
`0a6781d8cb8d9b8eb59245608dddee75ee9cf21b`, 622 UTF-8 bytes, with literal
SHA-256
`7a4a52d3c255798bb4928a12ca27222fc2f52f7a1005f6612a06a0e71b01a8f2`
and embedded self-hash
`605979eea4d844591e10f379c2e0e41ed14475e206924d830b0bfc5d58b8642e`.
It fixes `preflight_consumed=true`, `rerun_authorized=false`,
`development_authorized=false`, confirmation/final closed, and real money
unauthorized.

The immutable V3.15 implementation remains commit
`c3a83f1e676d3c8c80d0224954d2e9ccc95ba30a`, tree
`2206d5dddcddf1122c05ea167ceffccd03b3cc34`, whose sole parent is V3.15
preregistration commit `3955520674a3b6ccfa29cf9fc6593e110917e437`, tree
`c07f5893b235b145914cb115e1888fb360d59b34`. The preregistration document
blob is `0d00607ac6a8fc548987c038e951f0c831437097`, 24,926 bytes, with literal
SHA-256
`48af36ce45277be93fdb0af6e2a5173be725f8305f03ad956dec8e32cce37d65`.

The V3.15 comparison-only rejection record at the immutable successor base
contains `e/APPROACH_COMPARISON.md` as Git blob
`836a70209d86b113759f89963a13f5ef8f0c1902`, 34,082 bytes, with literal
SHA-256
`180de853d0aaaa9f1b50535ccfea195f6675da389eb78407f7b882b950cb5264`.

The V3.16 document-only preregistration commit is named `P316`. It must have
the V3.15 comparison-only rejection commit as its sole parent, add only this
document, and be pushed and live-remote authenticated before any V3.16
implementation file is created. Once pushed, `P316` is immutable.

## Exact inheritance and narrow replacements

V3.16 incorporates every normative V3.15 preregistration provision after the
exact eight-form mechanical mapping `V3.15 -> V3.16`, `v3.15 -> v3.16`,
`V3_15 -> V3_16`, `v3_15 -> v3_16`, `V3-15 -> V3-16`,
`v3-15 -> v3-16`, `V315 -> V316`, and `v315 -> v316`, plus the topology
mapping `P315/I315/F315/R315/S315/C315/X315 ->
P316/I316/F316/R316/S316/C316/X316`, except for the explicit replacements in
this document.

The mapping applies to branch, module, class, function, schema, namespace,
attempt, artifact, pending suffix, phase, command, safe-code, comparison-row,
and Git-topology identities. It never rewrites quoted V3.15 commits, trees,
blobs, hashes, byte counts, failure facts, scientific authority, preserved
V3.8/V3.9 evidence, or the two exact case-distinct Phase 2 node IDs.

Only these V3.15 provisions are replaced:

1. successor base, branch, ancestry, preregistration, implementation,
   namespace, attempt, artifact, schema, and topology identities;
2. the six production and six test path names;
3. the Phase 1 selector paths, minimum node count, and required node list;
4. the isolated qualification bootstrap bytes and hash, only to install the
   exact controlled six-name Windows recovery envelope below after the
   original 16-name environment and pinned pytest identity pass;
5. qualification contract-manifest authority, only to disclose the exact
   16-name launch, six controlled additions, and 22-name pytest stages below;
6. qualification regression coverage, only to execute the exact two unchanged
   Windows recovery nodes under that bootstrap and prove the eight shared
   recovery symbols remain unreachable from V3.16 production; and
7. revision-owned repository, dependency, loaded-code, runtime-authority, and
   public/private hashes, which must be derived afresh from pushed V3.16 bytes.

There are no other replacements. The literal 44-module preload, real-source
self-audit, strict local-import scanner, JUnit case mapping, exact count parity,
long-duration grammar, and duplicate-summary rejection remain unchanged. If
this document is silent, the mapped V3.15 rule remains exact. Weakening by
alias, inference, variable tracing, permissive receipt parsing, bypass,
diagnostic observation, or donor behavior is forbidden.

## Frozen science and chronological boundary

The authenticated scientific authority remains the exact V3.15 authority and
the exact V3.8 preserved source. The system uses all 75 development filings,
including exactly 73 real filename/official-primary-URL pairs and two honest
null/null pairs. The frozen 12-key scientific projection remains exactly
38,320 bytes with SHA-256
`1ee05d2916752752bbef3710d70c7dab664fb82b9ac22829dac8897401058609`.

Only the literal `development` experiment is authorized. The requested market
window remains 1998-01-01 through 2019-01-01 exclusive. Years 2000-2004 remain
warm-up/learning only. The five scored blocks remain 2005-2007, 2008-2010,
2011-2013, 2014-2016, and 2017-2018. Confirmation 2019-2023 and live-style
2024 onward remain unreachable unless every earlier gate passes and a new
later-stage preregistration is committed, pushed, and authenticated.

Every policy remains long AAPL or cash only. Exposure must remain in `[0,1]`;
cash may never be negative. Shorting, leverage, borrowing, margin, cash
interest, paid APIs, broker calls, and real-money execution remain forbidden.
V3.16 and AAPL buy-and-hold use the same starting cash, dates, adjusted-price
source, next-open fills, corporate-action treatment, ledger, valuation date,
and full precision. Costs remain 5 basis points per changing leg with a
10-basis-point stress result. Every inherited development gate must pass.

No historical strategy version is executed. Older-named Phase 2 and Phase 3
test files remain exact shared offline component checks for the latest V3.16
system; they do not run or reinterpret an older experiment.

## Exact twelve-file implementation boundary

After pushed `P316` exists, its implementation child `I316` must have `P316`
as its sole parent and add exactly these twelve paths:

1. `agent_benchmark/sec_gemma_lean_science_v316_contract.py`
2. `agent_benchmark/sec_gemma_lean_science_v316_bridge.py`
3. `agent_benchmark/sec_gemma_lean_science_v316_journal.py`
4. `agent_benchmark/sec_gemma_lean_science_v316_store.py`
5. `agent_benchmark/sec_gemma_lean_science_v316_preflight.py`
6. `agent_benchmark/sec_gemma_lean_science_v316_runner.py`
7. `tests/test_sec_gemma_lean_science_v316_contract.py`
8. `tests/test_sec_gemma_lean_science_v316_bridge.py`
9. `tests/test_sec_gemma_lean_science_v316_journal.py`
10. `tests/test_sec_gemma_lean_science_v316_store.py`
11. `tests/test_sec_gemma_lean_science_v316_preflight.py`
12. `tests/test_sec_gemma_lean_science_v316_runner.py`

No tracked predecessor path may change in `I316`. Before official preflight,
the local branch, upstream, cached and live remote branch, commit, tree, sole
parent, exact twelve additions, Git blobs, working literal hashes and byte
counts, predecessor equality, and clean worktree must authenticate.

No V3.16 production or Phase 1 test module may import or execute a V3.15,
V3.14, V3.13, V3.12, V3.11, or V3.10 production/test module or historical
strategy entry point. The six V3.16 production modules own successor behavior
and may import only one another, the exact inherited 37-module shared closure,
the package initializer, standard-library modules, and the exact qualified
third-party runtime.

## Exact literal 44-module preload

The strict scanner continues to recognize visible call names `__import__`,
`import_module`, and `run_module`. Every recognized call must have a positional
first argument that is an exact Python string literal. The scanner performs no
constant propagation or variable tracing and remains terminal on a missing or
nonliteral first argument.

The production preloader first calls
`importlib.import_module("agent_benchmark.sec_gemma_online_risk_overlay_source_verifier")`
and binds the returned exact verifier identity. It then constructs one literal
dictionary with the following 44 keys in this exact order. The verifier key is
bound to the already imported verifier object; each other value is produced by
`importlib.import_module("<the exact key>")` with the literal key written
directly inside that call:

1. `agent_benchmark.sec_gemma_lean_science_v316_contract`
2. `agent_benchmark.sec_gemma_lean_science_v316_bridge`
3. `agent_benchmark.sec_gemma_lean_science_v316_journal`
4. `agent_benchmark.sec_gemma_lean_science_v316_store`
5. `agent_benchmark.sec_gemma_lean_science_v316_preflight`
6. `agent_benchmark.sec_gemma_lean_science_v316_runner`
7. `agent_benchmark.sec_audit_transport`
8. `agent_benchmark.sec_filing_content`
9. `agent_benchmark.sec_filing_gemma_contract`
10. `agent_benchmark.sec_filing_gemma_corpus`
11. `agent_benchmark.sec_filing_gemma_extractor_prompt`
12. `agent_benchmark.sec_filing_gemma_extractor_schema`
13. `agent_benchmark.sec_filing_gemma_learner`
14. `agent_benchmark.sec_filing_gemma_market_acquirer`
15. `agent_benchmark.sec_filing_gemma_market_evidence`
16. `agent_benchmark.sec_filing_gemma_market_source_bytes`
17. `agent_benchmark.sec_filing_gemma_ollama`
18. `agent_benchmark.sec_filing_gemma_preprocessor`
19. `agent_benchmark.sec_gemma_lean_v38_journal`
20. `agent_benchmark.sec_gemma_lean_v38_source`
21. `agent_benchmark.sec_gemma_lean_v38_transport`
22. `agent_benchmark.sec_gemma_online_risk_overlay_acquisition`
23. `agent_benchmark.sec_gemma_online_risk_overlay_attempt`
24. `agent_benchmark.sec_gemma_online_risk_overlay_baseline`
25. `agent_benchmark.sec_gemma_online_risk_overlay_contract`
26. `agent_benchmark.sec_gemma_online_risk_overlay_features`
27. `agent_benchmark.sec_gemma_online_risk_overlay_learner`
28. `agent_benchmark.sec_gemma_online_risk_overlay_ledger`
29. `agent_benchmark.sec_gemma_online_risk_overlay_market_verifier`
30. `agent_benchmark.sec_gemma_online_risk_overlay_metrics`
31. `agent_benchmark.sec_gemma_online_risk_overlay_no_leverage`
32. `agent_benchmark.sec_gemma_online_risk_overlay_policy`
33. `agent_benchmark.sec_gemma_online_risk_overlay_production`
34. `agent_benchmark.sec_gemma_online_risk_overlay_publisher`
35. `agent_benchmark.sec_gemma_online_risk_overlay_registry`
36. `agent_benchmark.sec_gemma_online_risk_overlay_replay`
37. `agent_benchmark.sec_gemma_online_risk_overlay_runner`
38. `agent_benchmark.sec_gemma_online_risk_overlay_runtime`
39. `agent_benchmark.sec_gemma_online_risk_overlay_source_verifier`
40. `agent_benchmark.sec_gemma_online_risk_overlay_store`
41. `agent_benchmark.sec_gemma_online_risk_overlay_vault`
42. `agent_benchmark.sec_point_in_time`
43. `agent_benchmark.sec_session_calendar`
44. `agent_benchmark`

The derived path-to-module list must equal the literal dictionary key tuple
exactly before any later preload work. There are exactly 45 recognized dynamic
import calls across the six V3.16 production ASTs: the 44 exact module literals
above plus exact literal `numpy`. Every literal occurs once. The verifier-first
call supplies item 39 and is not repeated inside the dictionary.

No import alias, helper wrapper, lambda, variable argument, computed string,
formatted string, concatenation, default argument, `getattr` dispatch, scanner
exception, or scanner weakening is permitted. Module identities, preload trap,
network prohibition, private-body prohibition, thread rule, Requests identity,
numerical warm-up, pseudo-module normalization, and all later runtime rules
remain mapped V3.15 behavior.

## Real-source self-audit

Before `I316` is pushed, the implementation must run a source-level audit over
the six actual V3.16 working production files. It must parse their exact bytes,
find every recognized dynamic-import call, prove the 45-call literal multiset
above, and prove zero missing/nonliteral first arguments.

After the local `I316` commit exists and before push, the exact required test
`test_real_v316_production_import_calls_are_literal_and_upper_bound_closes`
must:

1. authenticate that the six working production bytes equal `HEAD`;
2. apply the same AST rule and exact 45-literal multiset to those committed
   bytes;
3. call `_audit_v316_local_import_upper_bound` on the real `HEAD` with the
   exact 44 allowed local runtime paths;
4. receive exactly the duplicate-free 44-member UTF-8-sorted permutation of
   mapped `LOCAL_IMPORT_ALLOWED_PATHS`, with equal set and cardinality; and
5. prove that replacing one literal local call with a variable-driven call in
   a detached synthetic source still rejects with
   `preflight_local_import_dynamic_unresolved`.

The test may not weaken or monkeypatch the production scanner. Failure blocks
push and official preflight.

## Exact controlled Windows qualification envelope

The V3.15 16-name launch environment, its path-redacted profile, and its
initial exact-name rejection remain unchanged. Real `USERPROFILE`,
`LOCALAPPDATA`, `APPDATA`, `HOME`, `COMSPEC`, and `PATHEXT` values are not
copied from the host. Only after the original environment, interpreter,
`sys.path`, repository, and pinned pytest identity have passed, and immediately
before `pytest.main(pytest_args)`, the qualification bootstrap adds exactly
this literal block:

```python
qualification_temp=os.path.realpath(os.environ['TEMP'],strict=True)
qualification_system_root=os.path.realpath(os.environ['SYSTEMROOT'],strict=True)
qualification_windir=os.path.realpath(os.environ['WINDIR'],strict=True)
qualification_tmp=os.path.realpath(os.environ['TMP'],strict=True)
qualification_comspec=os.path.join(qualification_system_root,'System32','cmd.exe')
if key(qualification_temp)!=key(os.environ['TEMP']) or key(qualification_tmp)!=key(qualification_temp) or key(qualification_system_root)!=key(os.environ['SYSTEMROOT']) or key(qualification_windir)!=key(qualification_system_root) or not os.path.isdir(qualification_temp) or not os.path.isdir(qualification_system_root) or key(os.path.realpath(qualification_comspec,strict=True))!=key(qualification_comspec) or not os.path.isfile(qualification_comspec):
    fail('bootstrap_windows_recovery_environment_invalid')
controlled_environment={'USERPROFILE':qualification_temp,'LOCALAPPDATA':qualification_temp,'APPDATA':qualification_temp,'HOME':qualification_temp,'COMSPEC':qualification_comspec,'PATHEXT':'.COM;.EXE;.BAT;.CMD'}
if any(name in os.environ for name in controlled_environment):
    fail('bootstrap_windows_recovery_environment_invalid')
os.environ.update(controlled_environment)
if tuple(sorted(os.environ))!=tuple(sorted((*env_names,*controlled_environment))) or any(os.environ.get(name)!=value for name,value in controlled_environment.items()):
    fail('bootstrap_windows_recovery_environment_invalid')
```

Thus `USERPROFILE`, `LOCALAPPDATA`, `APPDATA`, and `HOME` are the same already
authenticated canonical temporary directory; `COMSPEC` is the canonical
`System32/cmd.exe` under the already authenticated Windows root; and `PATHEXT`
is exactly `.COM;.EXE;.BAT;.CMD`. `TMP` must resolve to `TEMP`, `WINDIR` must
resolve to `SYSTEMROOT`, every required directory/file must exist, all six
names must be absent before installation, and the post-installation name set
must equal the original 16 names plus exactly these six with every exact
derived/fixed value preserved. Any mismatch rejects with
`bootstrap_windows_recovery_environment_invalid` before test collection.

The complete V3.16 qualification bootstrap is therefore exactly 5,482 UTF-8
bytes with SHA-256
`59aa7f29b7200e5a915b15a840da050e7803c825bf40ad42720dc0a4092e4407`.
The controlled values exist only inside the authenticated pytest child. They
authorize no production request or effect and add no real host-profile value
to public evidence. The inherited V3.15 long-duration grammar, exact JUnit
mapping, summary/count parity, duplicate-summary rejection, and all their
tests remain unchanged.

The V3.16 contract manifest must distinguish both authenticated stages. It
adds exact `QUALIFICATION_CONTROLLED_PYTEST_ENVIRONMENT` authority rows, in
this order:

1. `USERPROFILE -> canonical_TEMP`
2. `LOCALAPPDATA -> canonical_TEMP`
3. `APPDATA -> canonical_TEMP`
4. `HOME -> canonical_TEMP`
5. `COMSPEC -> canonical_SYSTEMROOT/System32/cmd.exe`
6. `PATHEXT -> .COM;.EXE;.BAT;.CMD`

It also adds `QUALIFICATION_PYTEST_ENVIRONMENT_NAMES`, the duplicate-free
UTF-8-byte-sorted union of the original 16 launch names and these six names.
`build_qualification_contract()` must publish `launch_names` as the original
16, `controlled_pytest_additions` as the six exact rows, and `pytest_names` as
the exact 22-name union. The private launch receipt continues to hash only the
16 actual process-launch values; the authenticated bootstrap bytes and these
manifest fields bind the deterministic 16-to-22 transition before pytest
loads any repository test module.

The exact new required test is
`test_isolated_qualifier_executes_exact_windows_recovery_nodes_in_controlled_environment`.
It must launch the frozen qualifier with a fresh JUnit path and exactly these
two selectors:

1. `tests/test_sec_gemma_online_risk_overlay_production.py::test_recovery_supervisor_uses_private_stdin_and_accepts_only_envelope`
2. `tests/test_sec_gemma_online_risk_overlay_production.py::test_recovery_private_request_restores_exact_git_host_environment`

It must prove the launch environment still contains only the original 16
names, exit zero, exact two-node collection/execution identity, two passed,
zero failed/errors/skipped/xfailed/xpassed, exact terminal-summary/JUnit parity,
and no raw controlled path in its redacted environment profile. The same
candidate literal already passed these two unchanged nodes together before
`P316`; that observation freezes the general correction but creates no
preflight reservation or execution authority. No private V3.15 file is a
runtime or test dependency.

The controlled envelope is a synthetic qualification fixture, not authority
for the real V3.16 scientific process to call the older shared publication-
recovery implementation. The outer scientific process keeps its exact
14-name environment. Inside the same required test, an AST audit over the six
real V3.16 production sources must prove that no `Name.id`, `Attribute.attr`,
imported alias name, or exact string constant equals any of these shared
recovery symbols:

1. `_validate_publication_recovery_host_environment`
2. `_publication_recovery_host_environment`
3. `_install_publication_recovery_host_environment`
4. `_build_publication_recovery_worker_request`
5. `_publication_recovery_worker_payload`
6. `_supervise_publication_recovery_worker`
7. `_recover_production_publication_from_entry`
8. `recover_production_publication`

The mapped V3.16 `recover_publication` remains its separate verification-only
path and is not one of these shared entry points. Any new reference to one of
the eight exact symbols rejects before `I316` may be pushed or preflight may
reserve.

## Exact qualification launcher and phases

The exact isolated qualification launcher remains CPython 3.12.2 with
`-s -S -B`, pytest 9.0.3, the exact 5,482-byte bootstrap and hash above, the
unchanged exact reduced 16-name launch environment, JUnit receipts,
collection/execution parity, marker-last durability, and 25-minute total
deadline. The latest phase name is exactly `v316`.

Qualification has exactly three fresh-process phases. No repository-wide
suite, historical CLI, old experiment, or external effect is permitted.

### Phase 1: V3.16 only

Run every collected node in the six exact V3.16 test files. Phase 1 must
collect at least 325 case-sensitive unique nodes, zero duplicates, and every
node must pass. It must include these 24 exact nodes:

1. `tests/test_sec_gemma_lean_science_v316_preflight.py::test_phase2_case_sensitive_multiplicity_is_655_unique_zero_duplicates`
2. `tests/test_sec_gemma_lean_science_v316_preflight.py::test_phase2_rejects_case_insensitive_node_id_collapsing`
3. `tests/test_sec_gemma_lean_science_v316_preflight.py::test_isolated_qualifier_bootstrap_matches_frozen_literal_and_imports_pinned_pytest`
4. `tests/test_sec_gemma_lean_science_v316_preflight.py::test_authority_creation_precedes_authority_consumption`
5. `tests/test_sec_gemma_lean_science_v316_preflight.py::test_authority_creation_never_requires_f_or_pushed_authority`
6. `tests/test_sec_gemma_lean_science_v316_runner.py::test_development_consumes_only_pushed_gated_authority`
7. `tests/test_sec_gemma_lean_science_v316_runner.py::test_continuation_authorized_precedes_attempt_open_binding`
8. `tests/test_sec_gemma_lean_science_v316_runner.py::test_continuation_attempt_open_precedes_model_continuation_pre`
9. `tests/test_sec_gemma_lean_science_v316_runner.py::test_model_continuation_pre_precedes_first_ollama_intent`
10. `tests/test_sec_gemma_lean_science_v316_store.py::test_recovery_api_is_reachable_end_to_end_with_ordered_demotion_and_verification_only`
11. `tests/test_sec_gemma_lean_science_v316_store.py::test_nested_mappingproxytype_evidence_is_recursively_thawed_and_canonicalized`
12. `tests/test_sec_gemma_lean_science_v316_runner.py::test_forbidden_old_validator_sentinel_is_retained_and_triggered`
13. `tests/test_sec_gemma_lean_science_v316_runner.py::test_pushed_result_gate_uses_candidate_material_schema_with_plan_equals_plan`
14. `tests/test_sec_gemma_lean_science_v316_preflight.py::test_runtime_owner_graph_has_no_discovered_pseudo_slots_or_unrooted_callables`
15. `tests/test_sec_gemma_lean_science_v316_preflight.py::test_runtime_owner_slots_disambiguate_every_same_natural_callable_identity`
16. `tests/test_sec_gemma_lean_science_v316_preflight.py::test_runtime_closure_state_allowlist_binds_exact_five_objects_and_slots`
17. `tests/test_sec_gemma_lean_science_v316_preflight.py::test_runtime_weak_registries_are_empty_callback_bound_and_identity_continuous`
18. `tests/test_sec_gemma_lean_science_v316_preflight.py::test_runtime_relative_path_allowlist_is_exact_and_alias_bound`
19. `tests/test_sec_gemma_lean_science_v316_preflight.py::test_runtime_typing_alias_allowlist_is_exact_and_identity_bound`
20. `tests/test_sec_gemma_lean_science_v316_preflight.py::test_runtime_pseudo_module_normalization_requires_all_four_once`
21. `tests/test_sec_gemma_lean_science_v316_preflight.py::test_loaded_code_v2_callable_rows_bind_owner_slot_hashes`
22. `tests/test_sec_gemma_lean_science_v316_preflight.py::test_real_v316_production_import_calls_are_literal_and_upper_bound_closes`
23. `tests/test_sec_gemma_lean_science_v316_preflight.py::test_junit_counts_accepts_exact_pytest_long_duration_clock_suffix`
24. `tests/test_sec_gemma_lean_science_v316_preflight.py::test_isolated_qualifier_executes_exact_windows_recovery_nodes_in_controlled_environment`

A missing, renamed, skipped, xfailed, xpassed, deselected, duplicated, failed,
errored, or timed-out required node rejects V3.16.

### Phase 2: exact shared dependencies

Run the same exact 14 ordered shared selectors. They must collect and pass
exactly 655 ordered nodes, 655 case-sensitive unique node IDs, zero duplicates,
and ordered node-list SHA-256
`1de8cc183af68c089aa5616f5e51405abda34382ff15e73123f5cd3c0dfea83c`.
The exact inherited 37-path shared closure and ordered 2,023-byte path-list
SHA-256
`0a1e7374b35677596418cfa82575bd6614df631eba34f1978ce9c9017a74aae9`
remain unchanged. Its revision-owned manifest must be recomputed for V3.16.

### Phase 3: isolated Requests identity

Run exactly
`tests/test_sec_gemma_lean_runner.py::test_runtime_modules_share_exact_verified_requests_identity`.
It must collect and pass exactly one unique node with ordered node-list SHA-256
`465bbb7fb1bd0633502006db2b84f6adab6ca7542d8c4e3d75b13d6cb7e73229`.

All phases require exit zero, exact collection/execution parity, marker-last
private receipts, and zero SEC, Yahoo, Ollama, Gemma, paid, broker, market,
performance, confirmation, live-style, or real-money effects.

## Runtime-v2 and authority lifecycle

The mapped V3.15 loaded-code-v2 rooted owner graph remains exact: natural
qualified names, four-field callable keys, complete `owner_slots_sha256`,
shortest canonical owner paths, exact five closure-owned objects, empty weak
registries with callback/self-reference continuity, exact four relative-Path
identities/five bindings, exact two typing identities/thirteen explicit roots,
four-row one-time pseudo-module normalization, live critical transport
identity rereads, and strong identity baselines at every checkpoint.

At clean pushed `I316`, preflight creation authenticates exactly
`7e124c309356fbba8c43868fd46c23ceb09137dd -> P316 -> I316`, runs the three
offline qualification phases, creates fresh V3.16 repository/dependency/
loaded-code/runtime authority under fakes, proves zero external effects, replays
the preserved source, and writes private authority marker-last. It must not
require a nonexistent `F316` or consume newly created authority.

The private namespace is `data/aapl_sec_gemma_lean_science_v3_16`. The fixed
attempt identity is `aapl-sec-gemma-lean-science-v3-16-development-001`.
Public paths are:

- `e/aapl_sec_gemma_lean_science_v3_16/DEVELOPMENT_PREFLIGHT.json`;
- `e/aapl_sec_gemma_lean_science_v3_16/DEVELOPMENT_PAUSE.json`;
- `e/aapl_sec_gemma_lean_science_v3_16/DEVELOPMENT_RESULT.json`;
- `e/APPROACH_COMPARISON.md`; and
- `docs/aapl_sec_gemma_lean_science_v3_16_continuation.md` only if required.

The one-shot preflight, pre-trust retry boundary, qualification order, source
replay, nullable projection, request sealing, privacy scans, zero-effect proof,
safe failure, marker-last public artifact, and pushed-result gate remain exact
after mapping. `F316` changes only the public preflight artifact, has `I316` as
its sole parent, and must be pushed before its separate read-only gate can
authorize development.

## Git topology, continuation, and terminal evidence

Every arrow is single-parent and every named commit must be pushed before it
authorizes the next step:

- normal result: `base -> P316 -> I316 -> F316 -> R316`;
- planned pause and result: `F316 -> S316 -> C316 -> R316`;
- consumed preflight rejection: `F316 -> X316`.

`R316` adds only the terminal result and changes the comparison table. `S316`
adds only the pause artifact. `C316` adds only the continuation document.
`X316` changes only the comparison table, creates no development result, and
authorizes no execution. The comparison table preserves every earlier row and
appends exactly one V3.16 safe outcome after V3.15.

Continuation still requires a pushed pause, a separate pushed continuation
preregistration, and fresh explicit user permission before
`continuation_authorized -> attempt_open -> model_continuation_pre -> first
continuation intent`. Publication recovery remains verification-only.

After any public result or rejection commit, the separate pushed-result gate
must authenticate branch/upstream, local/cached/live remote commit, tree,
parent, exact path delta, Git blobs, working bytes, private replay, public
hashes, comparison transition, effect counts, privacy, and clean worktree.

## One V3.16 development experiment only

Only after pushed `P316`, pushed and self-audited `I316`, passing pushed
`F316`, and every offline and pushed-result gate pass may the literal
`development` command start. Before it starts, report a simple runtime
estimate, prove offline that the six Yahoo requests can be constructed, and
state clearly that only V3.16 is running.

The one attempt reuses authenticated SEC evidence and therefore makes zero SEC
requests. A normal completed attempt has exactly six Yahoo requests, four local
Ollama identity requests, and 75 no-retry local Gemma generations. A correctly
paused and continued attempt has exactly eight identity requests. The inherited
30-second Yahoo, 60-second identity, and 900-second generation monotonic
deadlines remain exact. Every logical request has one physical request; no
retry, repair, pull, fallback, paid service, broker, or real-money effect is
allowed.

If any development gate fails, V3.16 is rejected and later stages remain
closed. Only if every development gate passes may a separately preregistered
confirmation stage open 2019-2023. Only if confirmation passes may a separately
preregistered live-style stage open 2024 onward. Historical inspection is never
prospective proof; reliable advantage claims require later prospective paper
trading.
