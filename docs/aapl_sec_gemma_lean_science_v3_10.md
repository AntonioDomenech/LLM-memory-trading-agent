# AAPL SEC/Gemma lean science bridge v3.10

## Status and exact ancestry

This document preregisters the only v3.10 approach before any v3.10
implementation, private projection, Yahoo request, Ollama request, Gemma
generation, market-value read, prediction, action, return, performance result,
broker interaction, or real-money action.

The branch is `codex/aapl-sec-gemma-lean-science-v3-10`. Its immutable starting
authority is:

| Item | Frozen value |
|---|---|
| successor base commit | `23a444d4f0b0e147823a33f48a7affcfce48d5a8` |
| successor base tree | `f405b14858db263d7185072c04623621c75e353d` |
| successor base parent | `29e7ecb4f3e42f3d50329e9d2337cd3eb91a49ee` |
| required preregistration parent | the successor base commit above |
| preregistration path | `docs/aapl_sec_gemma_lean_science_v3_10.md` |

This document must be the only changed path in the first v3.10 commit, and
that commit must be pushed before implementation begins. Once pushed, this
document is immutable. A correction requires a new version, branch, and
preregistration.

V3.10 is the corrected operational successor to v3.9. V3.9 never ran its
preflight and produced no scientific result. Its exact twelve-file offline
draft passed its 130 focused tests but was not eligible for promotion because
its preregistration incorrectly made every repository test part of the
scientific authorization gate. V3.10 tests the same still-unobserved
hypothesis; it is not tuned to a v3.9 performance result because no such result
exists.

The rejected v3.9 materials are provenance only:

| Item | Frozen value |
|---|---|
| v3.9 preregistration commit | `d50c33515ed9597b2fc07bb40c922a3f3166fbdd` |
| v3.9 offline draft commit | `030e30849364cd7f842c612b66ba68771dcfb7b1` |
| v3.9 rejection commit | `e063c1746dac468fd2d4c809f8b0e75750adad97` |

None of those commits authorizes v3.10, and no v3.9 attempt, state, artifact,
receipt, or result may be adopted.

## Exact inherited authority

The complete frozen source, science, chronology, model, request, ledger,
metric, gate, crash, privacy, and hard-falsifier rules are inherited from the
exact UTF-8 v3.9 preregistration document with this identity:

| Item | Frozen value |
|---|---|
| commit | `d50c33515ed9597b2fc07bb40c922a3f3166fbdd` |
| path | `docs/aapl_sec_gemma_lean_science_v3_9.md` |
| Git blob | `2b7042fd948cad91925c9fe9e01784ea0ff739c8` |
| literal SHA-256 | `feb9bd040e0ed65aed125e52e79e10bc8f87cd7e96776dc67353502fc0983446` |
| literal bytes | `43,098` |

For inherited rules only, the mechanical identity mapping is `V3.9` to
`V3.10`, `v3.9` to `v3.10`, `v3_9` to `v3_10`, and `v39` to `v310`. This
document replaces only the old status and branch ancestry, implementation
allowlist identities, repository-wide test gate, offline-test receipt,
preflight test step, public/private v3.10 paths, comparison-row ordering, and
all old v3.9 `PREREGISTRATION_*` identity values. The pushed v3.10 doc-only
commit defines the new preregistration commit, tree, parent, path, Git blob,
literal SHA-256, and byte count; the implementation must pin and authenticate
them. It must separately preserve the table above as
`V39_INHERITED_PREREG_*` authority rather than overwriting or confusing those
pins with v3.10 or v3.8 identity. Every numerical or scientific rule not
explicitly replaced here remains byte-for-byte and value-for-value
authoritative under the mechanical mapping.

In particular, v3.10 inherits unchanged:

- the authenticated 75-filing v3.8 development source and lossless streaming
  bridge;
- the exact 38,320-byte frozen science projection with SHA-256
  `1ee05d2916752752bbef3710d70c7dab664fb82b9ac22829dac8897401058609`;
- the model, model fingerprint, prompt, schema, temperature, seed, context,
  response bounds, no-retry rule, and runtime guards;
- the exact six-request Yahoo development batch;
- the five fixed pilots and strict projected-duration pause only when
  `projected_ns > 43_200_000_000_000`;
- the deterministic features, learners, policies, controls, labels, ledgers,
  5/10-basis-point costs, and no-leverage invariants;
- the one-shot, intent-first, marker-last, no-adoption crash rules;
- the exact effect budgets, privacy boundary, result preservation, later-stage
  locks, and all hard falsifiers.

## Separate successor and SEC-source identities

The successor code base and the SEC source evidence are different authorities
and must never be conflated.

The successor base is the `23a444d...` commit above. The immutable v3.8 SEC
source revision remains:

| Item | Frozen value |
|---|---|
| v3.8 source commit | `0c4b01cf5f1ef77548658d9bfa38e76fb1b70635` |
| v3.8 source tree | `68a9176ed22edeb2edc0f00ae1179c8724603539` |
| v3.8 source parent | `a0f971184ad26630478182be97f01c46c80498e1` |
| terminal Git blob | `28075db4e7cb85bfcdeb92e7b1aecce28f3a49fa` |
| terminal literal SHA-256 | `c176c6fb5e200dd363be656562d72bc40f722912221fe7f5cd52d8e42cc9723a` |
| source-authority Git blob | `d2d17dc2e1159e79bbcd2ca3f63cb733289b404f` |
| source-authority literal SHA-256 | `d7f87ac22e58ac4b50033af95e6f768692f1e31a6890e9f50e49f20d59ff0d0a` |
| v3.8 source-module Git blob | `b04263d5b3a8bdd74362fd02bc637d773d76ff9a` |
| v3.8 source-module literal SHA-256 | `8f624df2b28425b8e31db81997aeb0387d3c19418c0781e3b04fca66830d0f04` |
| v3.8 acquisition-module Git blob | `b5fbd94ecaff681ac698e47dd9ea0448d7886ee3` |
| v3.8 acquisition-module literal SHA-256 | `8ba5b3dee285a5e12e631c5d5e8fbafce1a66d1c81b4c5736371ac8ceffbb3f6` |

All other v3.8 public/private hashes, inventory totals, role counts, request
counts, journal receipts, seals, and replay pins remain exactly those in the
inherited authority. Implementation must expose separate constants for the
successor base and v3.8 source revision. A blanket replacement of the old
`BASE_*` constants is a terminal error.

The scientific parent remains commit
`efbc481c57e480d48303763163676e64e87df49d`, tree
`bc91d619a4d04680d9600b1acd74f0a29662d0f9`, and its exact frozen science
projection. Historical parent/source claims are authenticated at their pinned
revisions, not reinterpreted against newer HEAD files.

## Frozen hypothesis and development boundary

The primary hypothesis is unchanged:

> Fixed local Gemma extraction from all 75 authoritative development filings
> adds after-cost chronological value over the inherited fixed exhaustion
> baseline and over the quality-preserving no-filing-meaning control.

The bridge hypothesis is unchanged:

> The authenticated v3.8 development source can be transformed into the exact
> frozen scientific input without SEC reacquisition, row selection, omission,
> duplication, reordering, byte substitution, or a change to the science.

Only the literal command `development` is authorized. The market window is
1998-01-01 through 2019-01-01 exclusive. The 2000-2004 interval is warm-up and
learning only. The scored blocks remain:

| Block | First session | Last session |
|---:|---|---|
| 1 | 2005-01-03 | 2007-12-31 |
| 2 | 2008-01-02 | 2010-12-31 |
| 3 | 2011-01-03 | 2013-12-31 |
| 4 | 2014-01-02 | 2016-12-30 |
| 5 | 2017-01-03 | 2018-12-31 |

Confirmation 2019-2023 and live-style 2024 onward remain unreachable. A
development pass can only justify a separately preregistered confirmation
stage after the development result is committed, pushed, and independently
authenticated.

The 22 development gates remain exactly:

| Gate | Required value |
|---|---:|
| combined total active log edge at 10 bps | at least `0.02` |
| combined edge without best block at 10 bps | at least `0.005` |
| positive combined blocks at 10 bps | at least `4` of `5` |
| annual win rate at 10 bps | at least `0.55` |
| negative-AAPL-year win rate at 10 bps | at least `0.60` |
| complete SEC overlay episodes | at least `12` |
| overlay episode win rate at 10 bps | at least `0.55` |
| overlay median edge at 10 bps | strictly positive |
| largest positive episode share at 10 bps | at most `0.35` |
| schema-valid extraction rate | at least `0.90` |
| nonzero filing-meaning rows | at least `24` |
| incremental versus baseline at 10 bps | at least `0.005` |
| incremental versus baseline without best block | strictly positive |
| online versus block-frozen action differences | at least `5` |
| online versus block-frozen difference blocks | at least `3` |
| online versus block-frozen edge at 10 bps | strictly positive |
| semantic versus no-meaning action differences | at least `5` |
| semantic versus no-meaning difference blocks | at least `3` |
| semantic versus no-meaning complete XOR intervals | at least `4` |
| semantic versus no-meaning edge at 10 bps | at least `0.005` |
| semantic edge without best XOR interval | strictly positive |
| semantic Brier relative improvement | at least `0.01` |

Undefined, missing, nonfinite, unreconciled, zero-difference, or failed values
reject the approach. No subset, score, narrative judgment, or post-result
threshold change can turn failure into a pass.

## Exact implementation allowlist

After this document is committed and pushed alone, one implementation commit
may add only these production files:

1. `agent_benchmark/sec_gemma_lean_science_v310_contract.py`
2. `agent_benchmark/sec_gemma_lean_science_v310_bridge.py`
3. `agent_benchmark/sec_gemma_lean_science_v310_journal.py`
4. `agent_benchmark/sec_gemma_lean_science_v310_store.py`
5. `agent_benchmark/sec_gemma_lean_science_v310_preflight.py`
6. `agent_benchmark/sec_gemma_lean_science_v310_runner.py`

It may add only these test files:

1. `tests/test_sec_gemma_lean_science_v310_contract.py`
2. `tests/test_sec_gemma_lean_science_v310_bridge.py`
3. `tests/test_sec_gemma_lean_science_v310_journal.py`
4. `tests/test_sec_gemma_lean_science_v310_store.py`
5. `tests/test_sec_gemma_lean_science_v310_preflight.py`
6. `tests/test_sec_gemma_lean_science_v310_runner.py`

No existing tracked path may change in the implementation commit. The v3.9
draft may be used only as an audited text donor; it may not be cherry-picked,
made an ancestor, or copied with its old identities. The implementation must
split successor-base and v3.8-source constants, rename every v3.10 identity,
preserve the v3.9 rejection row, and place the later v3.10 comparison row after
it.

The six v3.10 test files must collect at least 130 tests and must cover every
requirement inherited from the v3.9 implementation list plus the split-base
authority, scoped qualification receipts, output durability, comparison
ordering, and every mismatch boundary. Production must never import a test
helper.

The implementation commit must have the pushed preregistration commit as its
only parent, add exactly the twelve paths above, and leave the worktree clean.
Its local HEAD, remote branch ref, commit, tree, parent, changed paths, Git
blobs, literal SHA-256 values, and predecessor equality must be authenticated
before preflight.

## Frozen v3.10 qualification suite

Only the frozen v3.10 qualification suite is scientific authorization. Every
collected node in it must pass. A repository-wide pytest run is maintenance
evidence only: its pass/fail status cannot by itself authorize or reject
v3.10 unless the same node is inside this frozen suite. However, an underlying
defect discovered anywhere must still reject v3.10 if it is independently
reproduced and violates any inherited science, chronology, privacy, ledger,
source, dependency, effect, or hard-falsifier rule.

Each phase runs in a fresh Python process with this exact environment and
pytest prefix:

```text
PYTHONDONTWRITEBYTECODE=1
PYTHONHASHSEED=0
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1
[authenticated sys.executable] -m pytest -q -p no:cacheprovider --junitxml=[private-preflight]/qualification/[phase]/[mode]/junit.xml.partial
```

The bracketed values have no caller choice. `[phase]` is exactly `v310`,
`dependencies`, or `requests_identity`; `[mode]` is exactly `collection` or
`execution`; and `[private-preflight]` is the authenticated v3.10 private
preflight namespace. The subprocess executable is the preflight
interpreter's `sys.executable` after its private absolute path and file bytes
pass these frozen identities:

| Item | Frozen value |
|---|---|
| Python version | `3.12.2 (tags/v3.12.2:6abddd9, Feb  6 2024, 21:26:36) [MSC v.1937 64 bit (AMD64)]` |
| Python cache tag | `cpython-312` |
| `os.name` | `nt` |
| `sys.platform` | `win32` |
| executable basename | `python.exe` |
| executable bytes | `103,192` |
| executable SHA-256 | `624bbc0586d8855633b875e911883bbef8a0e8b8711e11126df480dd86f54181` |
| pytest version | `9.0.3` |
| pytest `__init__.py` bytes | `5,582` |
| pytest `__init__.py` SHA-256 | `7be7a1e2218dc59a19d1ad131e4abe21172a295087efc72898938248782e8766` |

The private resolved executable and pytest paths are bound in the intent but
never published. A different interpreter, executable hash, Python identity,
pytest identity, platform, or package file fails before collection.

No `--lf`, xdist, shared pytest cache, plugin autoload, network plugin, or reuse
of a prior interpreter is allowed. Node selection within a listed file is
forbidden except for the one exact Phase 3 sentinel preregistered below.

### Phase 1: latest v3.10

Run all collected tests in these exact ordered files:

1. `tests/test_sec_gemma_lean_science_v310_contract.py`
2. `tests/test_sec_gemma_lean_science_v310_bridge.py`
3. `tests/test_sec_gemma_lean_science_v310_journal.py`
4. `tests/test_sec_gemma_lean_science_v310_store.py`
5. `tests/test_sec_gemma_lean_science_v310_preflight.py`
6. `tests/test_sec_gemma_lean_science_v310_runner.py`

The phase must collect at least 130 nodes and pass every node.

### Phase 2: actual shared dependencies

Run all 655 currently frozen nodes in these exact ordered files:

1. `tests/test_sec_filing_content.py`
2. `tests/test_sec_filing_gemma_contract.py`
3. `tests/test_sec_point_in_time.py`
4. `tests/test_sec_gemma_lean_v38_journal.py`
5. `tests/test_sec_gemma_lean_v38_source.py`
6. `tests/test_sec_gemma_lean_v38_transport.py`
7. `tests/test_sec_session_calendar.py`
8. `tests/test_sec_filing_gemma_ollama.py`
9. `tests/test_sec_gemma_online_risk_overlay_acquisition.py`
10. `tests/test_sec_gemma_online_risk_overlay_features.py`
11. `tests/test_sec_gemma_online_risk_overlay_production.py`
12. `tests/test_sec_gemma_online_risk_overlay_runner.py`
13. `tests/test_sec_gemma_online_risk_overlay_runtime.py`
14. `tests/test_sec_gemma_online_risk_overlay_contract.py`

The frozen Phase 2 node-list SHA-256 is
`1de8cc183af68c089aa5616f5e51405abda34382ff15e73123f5cd3c0dfea83c`.

These are the owning behavioral tests for the shared APIs v3.10 directly
consumes. Their frozen transitive local production closure is exactly these 37
paths in UTF-8 path-sort order:

1. `agent_benchmark/sec_audit_transport.py`
2. `agent_benchmark/sec_filing_content.py`
3. `agent_benchmark/sec_filing_gemma_contract.py`
4. `agent_benchmark/sec_filing_gemma_corpus.py`
5. `agent_benchmark/sec_filing_gemma_extractor_prompt.py`
6. `agent_benchmark/sec_filing_gemma_extractor_schema.py`
7. `agent_benchmark/sec_filing_gemma_learner.py`
8. `agent_benchmark/sec_filing_gemma_market_acquirer.py`
9. `agent_benchmark/sec_filing_gemma_market_evidence.py`
10. `agent_benchmark/sec_filing_gemma_market_source_bytes.py`
11. `agent_benchmark/sec_filing_gemma_ollama.py`
12. `agent_benchmark/sec_filing_gemma_preprocessor.py`
13. `agent_benchmark/sec_gemma_lean_v38_journal.py`
14. `agent_benchmark/sec_gemma_lean_v38_source.py`
15. `agent_benchmark/sec_gemma_lean_v38_transport.py`
16. `agent_benchmark/sec_gemma_online_risk_overlay_acquisition.py`
17. `agent_benchmark/sec_gemma_online_risk_overlay_attempt.py`
18. `agent_benchmark/sec_gemma_online_risk_overlay_baseline.py`
19. `agent_benchmark/sec_gemma_online_risk_overlay_contract.py`
20. `agent_benchmark/sec_gemma_online_risk_overlay_features.py`
21. `agent_benchmark/sec_gemma_online_risk_overlay_learner.py`
22. `agent_benchmark/sec_gemma_online_risk_overlay_ledger.py`
23. `agent_benchmark/sec_gemma_online_risk_overlay_market_verifier.py`
24. `agent_benchmark/sec_gemma_online_risk_overlay_metrics.py`
25. `agent_benchmark/sec_gemma_online_risk_overlay_no_leverage.py`
26. `agent_benchmark/sec_gemma_online_risk_overlay_policy.py`
27. `agent_benchmark/sec_gemma_online_risk_overlay_production.py`
28. `agent_benchmark/sec_gemma_online_risk_overlay_publisher.py`
29. `agent_benchmark/sec_gemma_online_risk_overlay_registry.py`
30. `agent_benchmark/sec_gemma_online_risk_overlay_replay.py`
31. `agent_benchmark/sec_gemma_online_risk_overlay_runner.py`
32. `agent_benchmark/sec_gemma_online_risk_overlay_runtime.py`
33. `agent_benchmark/sec_gemma_online_risk_overlay_source_verifier.py`
34. `agent_benchmark/sec_gemma_online_risk_overlay_store.py`
35. `agent_benchmark/sec_gemma_online_risk_overlay_vault.py`
36. `agent_benchmark/sec_point_in_time.py`
37. `agent_benchmark/sec_session_calendar.py`

The frozen derivation is named `python_ast_local_import_closure_v1`:

1. Candidate files are the tracked paths returned by `git ls-tree -r
   --name-only` for the successor base under `agent_benchmark`.
2. The ordered roots are exactly
   `sec_filing_content.py`, `sec_filing_gemma_contract.py`,
   `sec_gemma_lean_v38_journal.py`, `sec_gemma_lean_v38_source.py`,
   `sec_gemma_lean_v38_transport.py`, `sec_session_calendar.py`,
   `sec_filing_gemma_ollama.py`,
   `sec_gemma_online_risk_overlay_acquisition.py`,
   `sec_gemma_online_risk_overlay_features.py`,
   `sec_gemma_online_risk_overlay_production.py`,
   `sec_gemma_online_risk_overlay_runner.py`,
   `sec_gemma_online_risk_overlay_runtime.py`, and
   `sec_gemma_online_risk_overlay_contract.py`, each under
   `agent_benchmark/`. `tests/test_sec_point_in_time.py` is an additional
   owning test for the already-included transitive `sec_point_in_time.py`
   module.
3. The explicit dynamic local paths are
   `agent_benchmark/sec_filing_gemma_learner.py` and
   `agent_benchmark/sec_gemma_online_risk_overlay_production.py`.
4. Every source is read as raw bytes with `git show BASE:path` and parsed with
   Python AST. The walk follows `import agent_benchmark.*`, absolute
   `from agent_benchmark...`, and level-one relative imports when the resolved
   `.py` exists in the base tree, repeating to a fixed point.
5. Paths are unique and sorted by their UTF-8 bytes. Each path object has
   exactly `git_blob_sha1`, `literal_sha256`, and `path`, with hashes computed
   from the exact base blob and raw bytes.

The canonical manifest object has exactly `base_commit`, `base_tree`,
`closure_path_count`, `derivation`, `explicit_dynamic_local_paths`,
`ordered_roots`, `paths`, and `schema`; `schema` is
`aapl-sec-gemma-lean-science-v3-10-local-production-closure-v1`. Canonical JSON
uses UTF-8, `ensure_ascii=False`, sorted keys, separators `,` and `:`, and no
trailing newline. It is exactly 8,756 bytes with SHA-256
`fdf7f282ddebd74eeb1e8a18c68a216aec2edf72dce68eda06ee83107591ee46`.
The canonical JSON array of the 37 ordered path strings is exactly 2,023 bytes
with SHA-256
`0a1e7374b35677596418cfa82575bd6614df631eba34f1978ce9c9017a74aae9`.

The package `agent_benchmark/__init__.py` is outside this defined module
closure because the derivation follows resolved module imports, not package-
initializer execution. Exact predecessor-blob equality protects it
separately. The implementation must embed and independently reproduce the
manifest. Preflight rejects an added, missing, unmapped, dynamically reached,
reordered, or changed local module before an external effect. The other
indirect modules are consumed through the thirteen tested APIs; unrelated
historical-version test suites are not run.

At implementation authentication and again at preflight, all six v3.10
production blobs are parsed independently. Every resolved static local import
and every local import observed while their effect boundaries run under fakes
must belong to exactly the six allowlisted v3.10 production modules or the
frozen 37-module shared set. The separately protected
`agent_benchmark/__init__.py` is the only package-bootstrap exception. An
unresolved dynamic target or any other existing, generated, test, or foreign
local module is terminal. Thus the frozen shared closure is also a strict
upper bound on the code that is eventually written, not merely a description
of the expected donor roots.

### Phase 3: isolated Requests identity

Run exactly this current shared-runtime sentinel in its own fresh process:

```text
tests/test_sec_gemma_lean_runner.py::test_runtime_modules_share_exact_verified_requests_identity
```

It must collect and pass exactly one node. The other three baseline-repair
tests are not v3.10 dependencies and are intentionally maintenance evidence,
not scientific gates. The repaired frozen scientific-contract source-pin test
is already included in Phase 2.

The frozen Phase 3 node-list SHA-256 is
`465bbb7fb1bd0633502006db2b84f6adab6ca7542d8c4e3d75b13d6cb7e73229`.

Before every execution phase, a collection-only process using the same exact
environment, prefix, and paths must seal the ordered node IDs, count, and
SHA-256. The canonical node-list bytes are each collected node ID encoded as
UTF-8, joined in collection order with one `\n` byte between adjacent IDs and
no final newline. Summary, warning, and blank lines are excluded. Phase 2 must
reproduce exactly 655 nodes and its frozen hash; Phase 3 exactly one and its
frozen hash; Phase 1 at least 130 and exactly the node manifest sealed for the pushed
implementation. Any collection drift, deselection, xfail substitution,
unexpected skip, failure, error, timeout, or nonzero exit rejects v3.10.

The current clean successor base passed the original 624 shared API tests
inside a 655-test focused run (`655 passed in 280.22s`) and separately passed
all 31 point-in-time tests (`31 passed in 0.10s`) plus the isolated Requests
sentinel (`1 passed in 0.50s`) on 2026-07-18. That is base-readiness evidence
only, not a v3.10 preflight or scientific result. The complete qualification
is expected to take about 7-10 minutes. One 25-minute monotonic deadline is created before Phase 1 collection
and is inherited without reset by every collection, execution, receipt write,
fsync, validation, and aggregate step through final qualification sealing. A
timeout or deadline overrun fails closed.

## Durable private test receipts

The old v3.9 five-field offline-test report is not reused. V3.10 freezes these
replacement schema identities:

| Receipt | Schema |
|---|---|
| collection manifest | `aapl-sec-gemma-lean-science-v3-10-qualification-collection-v1` |
| phase intent | `aapl-sec-gemma-lean-science-v3-10-qualification-intent-v1` |
| durable phase result | `aapl-sec-gemma-lean-science-v3-10-qualification-result-v1` |
| private aggregate | `aapl-sec-gemma-lean-science-v3-10-qualification-private-aggregate-v1` |
| redacted public receipt | `aapl-sec-gemma-lean-science-v3-10-qualification-public-receipt-v1` |

The preflight must not discard stdout or stderr. Before each collection or
execution it exclusively creates and fsyncs a canonical intent containing the
exact argv, environment profile, repository commit/tree, clean-state hash,
phase, mode, and the one inherited deadline. Collection and execution use
distinct fail-if-exists paths keyed by phase and mode for intent, staging log,
final log, staging JUnit XML, final JUnit XML, result, and completion marker.
Each process binary-tees stdout and stderr to its private
`output.log.partial`, flushing and fsyncing as output arrives, and requests
built-in JUnit XML at `junit.xml.partial`. Neither staging nor final path may
exist before its unit begins.

On every success, failure, exception, or timeout it writes a canonical private
result with a required terminal reason: `completed`, `launch_error`,
`timeout`, or `exception`. It records start/end times, monotonic duration,
ordered node count/hash, log hash, and all available process evidence. A path
that never obtains a process return code records `exit_code=null`. A path that
cannot authenticate completed JUnit records `xml_present=false`,
`xml_sha256=null`, and each pass/fail/error/skip/xfail/xpass count as `null`,
rather than inventing values. A passing result requires terminal reason
`completed`, integer counts, zero failures/errors/skips/xfails/xpasses, exit
code zero, complete XML, and every frozen count/hash.

Completed staging log/XML payloads are fsynced and published to their final
names under the no-replace durability mode below before the canonical result
and marker-last completion are published. An open intent, surviving staging
payload, missing success-path XML, mismatched count/hash, path collision, or
absent completion marker fails closed and is never presented as a pass.

The frozen durability mode is
`windows_file_fsync_rename_noreplace_marker_last_v1`. Every payload is written
to a unique fail-if-exists staging file, flushed, `os.fsync`-ed, closed, and
byte/hash verified. After proving the final path absent, it is published with
Windows `os.rename`, whose existing-destination error is terminal, then
reopened and verified before a separately fsynced marker-last record is
published the same way. `os.replace`, overwrite, delete, truncate, or reuse is
forbidden. This Windows mode makes no directory-fsync claim. Any failed file
barrier, rename, reopen, verification, or marker write rejects the phase;
exceptions are never silently treated as unsupported.

The public receipt exposes only the frozen suite identity
`latest_approach_actual_dependencies_pinned_parent_authority`, aggregate
counts, durations, exit codes, and cryptographic hashes. It never exposes a
readable SEC contact, private path, source body, request body, model response,
market value, label, action, return, or performance result.

## One-shot zero-effect preflight

After the exact twelve-file implementation commit is pushed, one and only one
v3.10 preflight may run. It is a private read-and-hash pass with zero SEC,
Yahoo, Ollama, generation, market-value, prediction, action, performance,
broker, paid-service, confirmation, final, or real-money effect.

It must:

1. authenticate this pushed preregistration and exact pushed implementation;
2. prove the exact twelve-addition delta and unchanged predecessor blobs;
3. authenticate the split successor base, v3.8 source revision, all public and
   private v3.8 authority, unchanged inventory, and 37-module dependency
   closure;
4. build the exact streaming 75-row projection;
5. prove frozen legacy universe, record, prior-link, preprocessing, request,
   pilot-order, and science-projection parity;
6. prove confirmation and final remain unreachable;
7. prove exact effect budgets and crash transitions using fakes only;
8. run and durably seal all three qualification phases above;
9. run unchanged privacy scans over private and proposed public bytes; and
10. seal the private preflight manifest before emitting a redacted artifact.

The public artifact path is
`e/aapl_sec_gemma_lean_science_v3_10/DEVELOPMENT_PREFLIGHT.json`. The private
namespace is `data/aapl_sec_gemma_lean_science_v3_10`, with disjoint preflight
and development children. The fixed development attempt identity is
`aapl-sec-gemma-lean-science-v3-10-development-001`.

The preflight artifact is committed and pushed alone. A separate read-only
pushed-preflight gate must authenticate its remote commit, tree, parent, exact
one-path delta, Git blob, literal/internal hashes, implementation pins,
private replay, zero-effect counts, qualification receipts, privacy, and clean
worktree. Only that gate may authorize development.

A failed or indeterminate preflight is terminal for v3.10. It is preserved and
pushed and is not rerun, repaired, or converted into permission to proceed.

## Development effects, preservation, and later stages

V3.10 makes no SEC request and never mutates v3.8. A normally completed
development attempt has exactly six Yahoo requests, four Ollama identity HTTP
requests, 75 unique Gemma generations, zero retries/repairs/pulls/fallbacks,
zero paid calls, zero confirmation/final opens, and zero broker/real-money
effects. A correctly paused-and-resumed attempt has eight identity requests;
all other successful counts are unchanged. The preserved family SEC request
count remains exactly 964.

The pilot timing rule, pause artifact, fresh continuation permission, segment
guards, crash states, and result ancestry are exactly inherited with v3.10
identities. No request or generation is repeated after an open intent.

Every terminal development outcome is preserved at
`e/aapl_sec_gemma_lean_science_v3_10/DEVELOPMENT_RESULT.json`, and
`e/APPROACH_COMPARISON.md` is updated in the same terminal result commit. The
v3.10 row must follow and preserve the existing v3.9 rejection row. Failure,
rejection, pause, or indeterminacy may not be deleted or relabeled.

Only a passing development result plus a passing pushed-result gate can
justify a new confirmation preregistration. A development pass is promising
retrospective evidence, not reliable-money proof. Confirmation and live-style
periods remain locked, prospective paper trading remains required before a
reliable-money claim, and real-money execution remains outside this goal
without separate fresh authorization.

## Additional hard falsifiers

All inherited hard falsifiers remain active. V3.10 additionally fails
terminally for any of these process violations:

- conflating successor-base identity with v3.8 SEC-source identity;
- treating v3.9 or its draft as passed authority or adopting any v3.9 state;
- changing an existing tracked path in the implementation commit;
- running a selected subset instead of every collected node in a frozen phase;
- allowing the pass/fail status of a repository-wide test outside the frozen
  suite to rewrite the preregistered scientific decision, or ignoring an
  independently reproduced underlying hard-falsifier defect merely because
  its discovering test was outside the suite;
- failing to derive, pin, or reproduce the exact 37-module dependency closure;
- using a shared interpreter, pytest cache, plugin autoload, xdist, `--lf`, or
  an unsealed collection manifest;
- discarding, truncating, or falsely summarizing qualification output;
- accepting a partial log, open intent, absent marker, nonzero exit, timeout,
  unexpected skip/xfail, count drift, node-hash drift, or receipt-hash drift;
- any SEC/Yahoo/Ollama/model/market/performance effect during preflight; or
- any uncommitted, unpushed, dirty, out-of-allowlist, privacy-leaking, paid,
  broker, real-money, short, leveraged, borrowed, negative-cash, or
  exposure-above-one path.

Failures are evidence and must be preserved on this branch.
