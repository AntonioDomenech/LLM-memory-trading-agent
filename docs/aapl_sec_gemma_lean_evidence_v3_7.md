# AAPL SEC/Gemma lean evidence v3.7

## Status and immutable predecessor

This is a new, separately preregistered left-boundary chronology successor on
`codex/aapl-sec-gemma-lean-evidence-v3-7`. This document must be committed and
pushed by itself before any v3.7 implementation commit, programmatic SEC
request, Yahoo value, Gemma generation, performance calculation, confirmation
source, or final source is opened.

V3.6 is terminally rejected and preserved at commit
`6f6752416025b5200b7bcb2d3dc96e55f2c456aa`, tree
`234ea6ec46727b35ec7396b375032910b7824736`. Its public terminal artifact is
`e/aapl_sec_gemma_lean_evidence_v3_6/DEVELOPMENT_ACQUISITION.json`, Git blob
`3ff9ba1bddb9d2b5c17a464487592fcb679c59a6`, literal file SHA-256
`f0dda0cba14ddc73fc5c14f984b4384ce826ee078b0a394dbc19e36fe58bcc62`, and
internal acquisition SHA-256
`0f88de5e28a754b817f318d7603898ce1a94b1f4990503c76400d4aa97117436`.
The comparison update in that terminal commit has Git blob
`2b64d160929072216fd1fbd6d4ebe8f7c28ed7f6`. V3.7 never resumes, repairs in
place, or reclassifies that attempt.

The v3.6 preregistration is pinned at commit
`18e3c47b4e3b29449d95a241b0989d60487c623f`, tree
`b75ded8fcddf81f80a0a0f4d17a150d47d2c260d`, document blob
`81dea7bddb023716e2615302a31e9be88743b3d0`, and document SHA-256
`8ef18188b1813f0ab759fc705205b528bc39fddf56472f15b44da9d1bfa99d73`.
Its implementation is pinned at commit
`19ca36450e55bc127cd8edb3eac0a991efbe8989`, tree
`b2f71724d24e4db8a9b2e6118d6eb34650f92691`, with committed-blob delta
manifest SHA-256
`05ea86a4f5ffeb48a8edae23b006d9b012b29193a190ba8bd3121b82cc6fa784`.
Its one-shot zero-source-effect preflight is pinned at evidence commit
`858638e5ede597a6611344b3ecc205b401690267`, tree
`77dad17ab3db04cd4ed8beb200398fc5ce4636b8`, public artifact blob
`1dce2b2d6061fb9472c9e0b29a40b7c607cf411a`, preflight SHA-256
`06dd5a91ce5149c7717da0728b50625ca215c6b0fe117eafa323da398e02367b`, and
literal file SHA-256
`f29f64f5115001b6cb6af865a5a0b5feac75593aaadd786fcc2ffc903dfbfb59`.
The executed v3.6 production-source inventory SHA-256 is
`ef1b2462a46daddabdf1242a40e51e5a51392904388ae45f3964c3fac549e7b0`.
V3.6 has no checkpoint, complete development source seal, public source
authority, or resumable attempt. The original scientific parent remains
`efbc481c57e480d48303763163676e64e87df49d`.

## Public evidence already seen and evidence still closed

V3.6 made exactly 199 fresh official SEC requests, all returning HTTP 200. It
sealed all 199 planned roles: two Apple Submissions roles, 100 quarterly
master roles from 1994-Q1 through 2018-Q4, and 97 complete-submission roles.
It had no failed role, no open intent, no unsealed response, and no checkpoint.
Deterministic source finalization then rejected with
`Frozen admitted D range/cap is violated`. The public terminal evidence records
`I=97`, `P=97`, `D=97`, 23 Forms 10-K, 74 Forms 10-Q, and exact acceptance for
all 97 development rows. It does not publish private row dates or bodies.

The v3.6 source adapter searches for the first availability session only in
the frozen 2000-onward experiment calendar. Its first session is
`2000-01-03`. Therefore a genuinely pre-stage evidence boundary whose true
next session is before 2000 can collapse onto `2000-01-03` and be labelled
development even though it is only an older verification source. The committed
synthetic v3.6 test explicitly demonstrates that behavior for a 1994 row. This
mechanism is public source-code evidence. The exact number of official v3.7
rows affected is unknown and must remain unknown until a fresh v3.7
acquisition.

The tempting repair `d_max=128` is rejected before implementation. The frozen
scientific contract permits at most 80 development model calls, requires one
call for every member of the complete development universe, and forbids
sampling. Raising only the source cap would create a source result that the
unchanged science must reject. Raising the model cap, trimming filings, or
sampling would be a broader performance-relevant scientific change and is not
allowed.

Every v3 through v3.6 SEC byte, receipt, manifest, journal event, seal,
checkpoint, typed row, probe marker, or diagnostic is predecessor evidence
only. V3.7 must reacquire every role into fresh namespaces. V3.7 production or
tests may use only the public terminal artifact, committed predecessor code,
committed tests, and committed documentation. They may not read, import,
compare, copy, parse, or reseal any private v3.6 response, journal, role blob,
parse receipt, manifest, checkpoint path, or diagnostic output.

Before v3.7, this approach family has made exactly 566 official SEC requests:
v3 made 4, v3.1 made 2, v3.2 made 15, v3.3 made 103, v3.4 made 109, v3.5 made
134, and v3.6 made 199. Every v3.7 public preservation must report v3.7
effects separately and the exact cumulative family total as
`566 + v3.7 actual official SEC requests`. Every durable v3.7 SEC role intent
counts once, including a request that fails or produces no usable response.
The two permitted loopback identity calls are not SEC requests and must be
reported separately.

## Frozen hypothesis and unchanged science

The scientific question remains literal and unchanged:

> Can a fixed local Gemma reader extract point-in-time deterioration evidence
> from Apple 10-K and 10-Q filings that helps a chronological long-or-cash
> learner avoid enough harmful AAPL exposure to add after-cost value over the
> frozen exhaustion baseline and a no-filing-meaning control?

V3.7 inherits every scientific rule from the pinned predecessor documents:

- model identity, model fingerprint, prompt, schema, seed, temperature,
  context, output caps, and the development model-call cap of 80;
- the twelve filing values, market features, and missingness flags;
- learner, fitting, threshold, interval, non-overlap, and next-open rules;
- inherited exhaustion baseline, semantic control, and no-meaning control;
- development, confirmation, final, and frozen-learning gates;
- 5-bps and 10-bps same-ledger AAPL buy-and-hold comparisons; and
- AAPL-or-cash only, no shorting, no leverage, no borrowing, no negative cash,
  no paid API, no real-money action, and no future leakage.

`H`, `I`, `P`, `Q`, `U`, `D`, every cap, the 95% acceptance requirement,
admitted counts, chronological stage windows, stage ordering, and every
performance threshold remain unchanged. No filing deletion, observed-row
exception, prompt rewrite, feature change, class rebalance, model swap,
threshold search, alternate horizon, or performance-informed repair is
allowed.

The only new source hypothesis is:

> V3.6 exceeded the frozen development cohort because it searched causal
> availability only in the 2000-onward experiment calendar, causing older
> evidence boundaries to collapse onto that calendar's first session. V3.7
> keeps the frozen 72-80 development cohort and 80-call science cap, but uses
> the already-frozen 1998-onward market-history calendar only for availability
> search. Its 2000-onward suffix is exactly the unchanged experiment calendar.
> Pre-stage filings remain fully authenticated source evidence but do not
> become development science merely because the experiment calendar has a
> left edge.

This is a falsifiable chronology hypothesis. It does not assert that fresh
official v3.7 data will produce `72 <= D <= 80` or pass any other source gate.
If it does not, v3.7 is terminally rejected rather than retuned.

## Exact left-boundary availability rule

The shared calendar module is frozen at v3.6 terminal Git blob
`7242b803e1d3b909abf40133fb2de2534189cabf`, literal SHA-256
`9a463fa0453440dc777a850e7af934dfc0f2740243e7e8e05ff6ebc430262f71`.
V3.7 may import its existing market-history tuple and validator locally but
must not modify that shared file or its tests.

The caller, bundle replay state, and science continue to use exactly the
frozen experiment calendar:

- calendar ID `nyse_trading_session_dates_2000_01_01_2026_07_10_v2`;
- covered interval `2000-01-01` through `2026-07-10`;
- 6,669 sessions, first `2000-01-03`, last `2026-07-10`;
- newline SHA-256
  `f3ea99a9fcbe99187701acb030cd6fd0f6dc2c7be53528ef7f5adb13a1c5fdd3`;
  and
- canonical-JSON SHA-256
  `e0550f12f98d7e0cf38d6797ae0d0e410bb3f9006793830ed75e0f753ccc5cf9`.

Availability search uses exactly the already-frozen market-history calendar:

- calendar ID `nyse_trading_session_dates_1998_01_01_2026_07_10_v1`;
- covered interval `1998-01-01` through `2026-07-10`;
- 7,173 sessions, first `1998-01-02`, last `2026-07-10`;
- newline SHA-256
  `df335e3d907a517e4072bbbc005433d7ff4cb930631740849b412700989d7ae6`;
  and
- canonical-JSON SHA-256
  `e9d37d63d158f8a3b6de58ef81970b27bcac9edb6a9c7b9e2f3ffe477d2f3032`.

The full tuples, not only their hashes or sets, must prove that the first 504
history sessions are the pre-2000 prefix and
`EXPECTED_MARKET_HISTORY_SESSIONS[504:] == EXPECTED_SESSIONS`. Both frozen
validators must pass. The caller may still supply only exact
`EXPECTED_SESSIONS`; caller-supplied history sessions, a subset, a superset, a
set-equivalent sequence, or any alternate tuple rejects.

For each filing, the unchanged evidence boundary is the latest of filing date,
exact acceptance date when present, and authenticated `DATE AS OF CHANGE` when
present. V3.7 then applies exactly this rule:

1. If the boundary is earlier than `1997-12-31`, the exact next market session
   is outside supported calendar history. The row is left-censored with
   `availability_session=null` and `stage_assignment=null`. Its original
   boundary and all source evidence remain sealed.
2. If the boundary is `1997-12-31` or later, availability is the first member
   of the frozen market-history tuple strictly greater than the boundary.
3. The unchanged stage windows are then applied. A 1998 or 1999 availability
   has `stage_assignment=null`. It remains in `I`; if newly acquired in that
   stage it also remains in current `P`, while a carried row remains outside
   current `P`. It cannot enter `U`, `D`, the science projection, model calls,
   lessons, labels, or performance.
4. A boundary of `1999-12-30` maps to `1999-12-31` and remains pre-stage. A
   boundary of `1999-12-31` maps to `2000-01-03` and enters development. An old
   filing with a later authenticated change date is treated by that later date
   under the same ordinary rule.
5. A boundary on or after the frozen calendar's final session retains the
   inherited `availability_session=null`, `stage_assignment=null` behavior.

The `1997-12-31` exactness threshold is derived from the validated history
receipt's `start=1998-01-01` minus one calendar day. It is not an observed-row
exception. A row whose final evidence boundary is earlier than `1997-12-31`
must never be described as having an exact 1998 availability date:
`1998-01-02` would only be the first supported sentinel, not the real next
session after a 1994 filing. V3.7 therefore stores null for unsupported
prehistory rather than making that false claim.

No new row flag is needed. The sealed `availability_not_before_date`, null
availability, null stage, and the receipt-level censor rule together determine
the state exactly. Seal validation and detached replay must recompute the same
state from source dates and the frozen calendars.

## Composite calendar receipt

The existing top-level source-seal key `session_calendar` remains, but v3.7
changes its value to one compact composite receipt with exactly these keys:

- `schema_version` equal to
  `aapl-sec-gemma-lean-v37-authoritative-calendar-receipt-v1`;
- `experiment_calendar`, containing the six compact identity fields from the
  validated 2000-onward calendar;
- `availability_search_calendar`, containing the same six compact identity
  fields from the validated 1998-onward calendar;
- `suffix_relation`, binding the exact ordered suffix relation, prefix count
  504, suffix start 504, suffix count 6,669, and both experiment-calendar
  session hashes;
- `availability_semantics`, binding the max-source-date boundary, strict `>`,
  derived exact-boundary start, null prehistory rule, separate search/science
  calendar roles, and unchanged stage start; and
- `authoritative_calendar_receipt_sha256`, the canonical SHA-256 of the prior
  five-field body.

Both shared validators return their exact seven-field evidence including the
full session arrays. The local receipt builder must require that exact schema,
prove the full tuple relation, and then omit the full arrays from the public
seal. Seal validation compares the supplied receipt with the exact
module-import-authenticated authoritative receipt; a merely self-consistent
forged hash never suffices.

## Frozen stage configurations and caps

No stage, model-call, request, byte, or time bound changes from v3.6:

| Stage | Availability window | Master coverage | `Q` | `I` cap | `P` cap | Admitted `D` | Maximum requests |
|---|---|---:|---:|---:|---:|---:|---:|
| Development | 2000-01-01 through 2018-12-31 | 1994 Q1 through 2018 Q4 | 100 | 128 | 128 | 72-80 | 245 |
| Intermediate | 2019-01-01 through 2023-12-31 | 1994 Q1 through 2023 Q4 | 120 | 160 | 24 | 19-20 | 161 |
| Final | 2024-01-01 through 2026-07-09 | 1994 Q1 through 2026 Q3, filtered at the cutoff | 131 | 176 | 16 | at most 12 plus every inherited completed-year rule | 164 |

A passing stage still has exactly `1 + H + Q + P` successful official SEC
requests. `H <= 16`; lifetime intents remain capped at 256 per stage. The
individual, aggregate successful-response, aggregate decompressed-master,
lifetime received-byte, `L + 1` reservation, TLS, privacy, timeout,
pause/resume, one-intent, no-retry, and crash-open rules remain exactly those
of v3.6. The fixed final cutoff does not advance with the calendar. V3.6's
observed development formula of 199 is predecessor evidence only; v3.7 must
rebuild `H`, `I`, `P`, and its request formula from fresh responses.

The frozen shared science contract is Git blob
`897286a72be5af2df3e5d59655838ef92364b086`, literal SHA-256
`a51a96a9b763fd3f16cb6461b7dfca58d88cafd5862cff7cc5d18d990994a68e`.
It and every other model, prompt, feature, market, learner, ledger,
performance, stage-access, and authorization path remain byte-unchanged.

## Canonical v3.7 sequence

After all Git and preflight gates below pass, development runs in this order:

1. reacquire the main Apple Submissions JSON once;
2. reacquire every referenced historical Submissions file once in canonical
   filename order;
3. build rejection-only pre-master `I` and `P` upper bounds;
4. fetch every master from 1994-Q1 through 2018-Q4 in canonical order;
5. retain every structurally valid row under the unchanged master rule;
6. exact-set reconcile the complete Apple Submissions/master target set;
7. fetch every required complete submission in `(filing_date, accession)`
   order from the exact master filename;
8. parse each original body under the unchanged source, envelope, raw-form,
   acceptance, topology, and byte-identity rules;
9. derive the unchanged maximum source-date boundary, apply the exact v3.7
   history-calendar/censor rule, and apply the unchanged stage windows;
10. finalize the complete development source seal and exact detached replay;
11. only then allow inherited development Yahoo values, fixed Gemma requests,
    features, labels, ledgers, or performance calculations.

Intermediate remains forbidden unless development passes its unchanged
scientific gate. Final remains forbidden unless intermediate passes. V3.7
reacquires its own response-time evidence; all predecessor bytes remain
diagnostic source-only evidence.

## Implementation and committed-blob proof

The mandatory sequence is:

1. commit and push this document alone;
2. add the exact v3.7 implementation and focused offline tests;
3. commit and push that implementation;
4. run one fresh zero-source-effect v3.7 preflight;
5. commit and push the preflight receipt alone;
6. only then make the first v3.7 SEC request;
7. preserve and push either the terminal rejection or complete source seal
   before any scientific stage can open.

The implementation commit may add exactly these twelve regular `100644`
files:

- `agent_benchmark/sec_gemma_lean_v37_acquisition.py`;
- `agent_benchmark/sec_gemma_lean_v37_delta.py`;
- `agent_benchmark/sec_gemma_lean_v37_journal.py`;
- `agent_benchmark/sec_gemma_lean_v37_preflight.py`;
- `agent_benchmark/sec_gemma_lean_v37_source.py`;
- `agent_benchmark/sec_gemma_lean_v37_transport.py`;
- `tests/test_sec_gemma_lean_v37_acquisition.py`;
- `tests/test_sec_gemma_lean_v37_delta.py`;
- `tests/test_sec_gemma_lean_v37_journal.py`;
- `tests/test_sec_gemma_lean_v37_preflight.py`;
- `tests/test_sec_gemma_lean_v37_source.py`; and
- `tests/test_sec_gemma_lean_v37_transport.py`.

No existing tracked path may change. Relative to the preregistration commit,
the implementation delta must therefore contain exactly twelve `A` records
and zero `M` records, with no rename, copy, deletion, type change, submodule,
or nonregular mode.

Each added file must be derived from its exact committed v3.6 counterpart at
the pinned terminal commit. The ordered mechanical namespace replacements are
`sec_gemma_lean_v36` to `sec_gemma_lean_v37`, `SecGemmaLeanV36` to
`SecGemmaLeanV37`, `V36` to `V37`, `v36` to `v37`, `v3_6` to `v3_7`,
`v3-6` to `v3-7`, and `v3.6` to `v3.7`.

After normalization, the only production changes allowed outside the new
delta verifier are in `agent_benchmark/sec_gemma_lean_v37_source.py`:

- changed `assignment|$module._EXPECTED_SESSION_SET|1`, from the experiment
  tuple to the validated history tuple;
- changed `function|_compact_authoritative_calendar_receipt|1`, to validate
  and compact both calendars, prove the exact suffix, bind the censor rule,
  and self-hash the composite receipt;
- added `function|_conservative_availability_session|1`, implementing only the
  preregistered left-censor and strict-next-history-session rule;
- changed `function|reconcile_complete_submission|1`, replacing only the
  direct experiment-calendar search with that helper;
- changed `function|_validate_target_row_binding|1`, recomputing the identical
  helper result during seal validation and replay; and
- changed `import|$module.__import__|13` to import exactly
  `EXPECTED_MARKET_HISTORY_SESSIONS`, `EXPECTED_SESSIONS`,
  `validate_aapl_session_calendar`, and
  `validate_market_history_session_calendar` from the unchanged shared
  calendar module.

No other production function, helper, class, decorator, assignment, import,
or call site may change. In particular `_canonical_sessions` must continue to
accept only exact caller `EXPECTED_SESSIONS`, and `StageSourceBundle` must
continue to store that experiment tuple for detached replay. The stage
configurations, `d_min=72`, `d_max=80`, stage windows, global availability
start, boundary inputs, strict `>`, row schemas, source parsing, acceptance
logic, request planning, and science projection remain unchanged.

The cloned v3.7 source test may change exactly these three existing physical
symbols, with no source-test import, other helper or fixture, decorator,
removal, or new test function:

- `function|_synthetic_full_rehearsal_inputs|1`, changing only its now-false
  left-boundary comments;
- `function|test_synthetic_full_size_three_stage_rehearsal_replay_and_prefix|1`,
  changing synthetic development `U/D/exact_acceptance_U` from 73 to 72,
  requiring the 1994 row to have null availability and null stage, and proving
  full replay plus unchanged later-stage counts; and
- `function|test_authoritative_calendar_is_exact_and_compact_identity_is_sealed|1`,
  proving both exact calendar receipts, the 504-session ordered suffix, the
  derived prehistory threshold, boundary cases, current-only caller input,
  rejection of caller history input, and hostile composite-receipt mutation.

The calendar test must cover far prehistory, `1997-12-30`, `1997-12-31`,
1998/1999 ordinary boundaries, `1999-12-30`, `1999-12-31`, weekends/holidays,
an old filing with a later authenticated change date, plus equality at the
frozen final session. It must prove that null/pre-stage rows never enter `U/D`
or the development projection, while synthetic `D=81` still rejects under the
unchanged cap. Full and compact
three-stage replay must remain exact. Intermediate `D=19`, final `D=12`,
`I/P`, request formulas, acceptance rules, and completed-year coverage remain
unchanged.

After normalization, acquisition, journal, preflight, and transport plus their
four corresponding test files must have empty changed-symbol and changed-import
sets. No shared calendar, shared parser, model, prompt, feature, learner,
market, ledger, performance, stage, or authorization file may change.

The delta module may change only preregistration/base pins, predecessor
replacement data, exact path/raw/symbol/import/evidence metadata, predecessor
labels, and cross-anchor metadata; the inherited direct-parent and
strict-addition validation remains unchanged, and it may not add a nested
helper. Excluding its self-referential
`assignment|$module._COUNTERPART_RULE_DATA|1`, its admitted normalized
identifiers are limited to the v3.7 equivalents of the inherited pin/path-rule
assignments. The normalized `_DEFAULT_COUNTERPART_RULES` assignment and
`build_delta_manifest` body must remain unchanged. No checker class,
counterpart helper, or other checker function body may change.

The delta test may change only its exact predecessor/preregistration fixtures,
the excluded `assignment|$module.V37_DELTA_MODULE_SHA256|1`, and one new
hostile test,
`function|test_checker_rejects_calendar_suffix_weakening_caller_broadening_and_left_edge_special_case|1`.
That hostile test must reject a source candidate that raises `d_max`, changes
a science cap, samples/truncates/drops by identity, uses a literal `D=97`
exception, changes `>` to `>=`, changes stage windows or the global stage
start, uses filing date instead of the full boundary, clamps all old rows to a
sentinel, special-cases one old identity/date/accession, accepts caller history
sessions, stores history in replay state, uses only end dates/counts/sets
instead of an ordered suffix, omits the composite receipt, edits the shared
calendar, or changes only initial parsing without changing seal validation.

The inherited inventory assertion must bind exactly twelve path rules, twelve
counterpart rules, the exact twelve-path `_V37_IMPLEMENTATION_PATHS` set,
exactly twelve `A` paths, and no `M` path. Every path begins with no prior blob
in the preregistration tree. The expected counterpart table maps every v3.7
path to its exact v3.6 path at the pinned terminal base.

The v3.7 delta checker must operate only on committed Git objects and ignore
all worktree bytes. It must use twelve exact counterpart rules. After the
seven ordered namespace replacements, the source counterpart may differ only
in the one assignment, three changed functions, one added function, and one
import listed above. The source-test counterpart may differ only in the three
existing functions listed above. The eight ordinary non-delta, non-source
files must be mechanically identical. The checker must bind all import
inventories, full position-free symbol evidence, raw framing, replacement
counts, exact changed-symbol sets, physical hashes, literal hashes, semantic
hashes, and complete raw SHA-256 values.

The preregistration commit must have exactly one direct parent, the pinned
v3.6 terminal commit, and exactly one added document. The implementation commit
must have exactly one direct parent, the preregistration commit, and exactly
the twelve strict additions above. Neither may be a merge.

The checker must bind all complete ordinary v3.7 blobs directly. The delta
module and its test must use the inherited non-circular cross-anchor design:
the test stores the raw delta-module SHA-256 in `V37_DELTA_MODULE_SHA256`; the
delta module stores the test SHA-256 with only that anchor's 64 digest
characters masked to zero; and canonical syntax plus every unmasked byte is
exact. Same-name/import body changes, comments, whitespace, anchor syntax, raw
framing, baseline pins, replacement counts, symbol sets, status records,
evidence hashes, censor semantics, and suffix relation must all be
hostile-tested and rejected.

The legacy manifest field name
`committed_v32_counterpart_comparisons` remains unchanged solely for
manifest-schema and inherited focused-test compatibility. Its twelve entries
must truthfully bind v3.6 counterparts. It does not make v3.2 or any other
predecessor a v3.7 implementation base.

## Fresh namespace and zero-effect gate

The fresh private checkpoint namespace is
`data/aapl_sec_gemma_lean_evidence_v3_7`; public evidence is under
`e/aapl_sec_gemma_lean_evidence_v3_7`. No v3.7 run artifact, marker, ledger,
journal, receipt, blob, manifest, parse receipt, checkpoint, or public
authority may exist before its defined step. Symlinks, hardlinks, reparse
points, copied predecessor schemas, renamed artifacts, and cross-version
resealing cannot satisfy freshness. The private SEC contact remains readable
only through the inherited ignored local configuration; public evidence may
contain at most its cryptographic fingerprint.

The v3.7 preflight requires a clean branch equal to
`origin/codex/aapl-sec-gemma-lean-evidence-v3-7`, this preregistration as an
ancestor, the exact pushed implementation commit/tree, raw worktree bytes
equal to committed source blobs, no active run, no prior v3.7 probe marker, a
safe writable ignored checkpoint root, and the unchanged private contact. It
must bind its own inherited loaded-source closure and the committed delta
manifest. The later acquisition gate must additionally bind every raw
production-module blob in its unchanged production closure, including the
frozen shared calendar, source parser, and point-in-time parser blobs from the
v3.6 terminal base.

The preflight may make exactly two loopback identity calls in order:
`GET /api/tags`, then `POST /api/show` for `gemma4:12b`. It makes no
`/api/version`, chat, generation, SEC, Yahoo, market, performance, broker, or
trade call. Intent, completion, and publish markers are durable and
non-retryable. Its public receipt must be the only tracked change in its own
commit and must be pushed before acquisition. The v3.7 identity probe may not
be rerun after any durable identity intent, including a probe failure, or
after success. A zero-effect local repository or private-contact gate failure
before any durable identity intent may be corrected and retried.

The production acquisition gate must revalidate that committed receipt,
implementation commit/tree, delta manifest, source inventory,
branch/upstream, clean worktree, a v3.7-only private namespace containing the
authenticated identity-intent, identity-completion, and preflight-publish
markers plus either no acquisition inventory or, on clean continuation, only
its own authenticated sealed-prefix state, contact fingerprint, and exact
zero-effect request sequence. It must fail closed without an SEC request on
any mismatch. A v3.7 role may be satisfied only by a fresh v3.7 intent,
response, transport receipt, blob, parse receipt, manifest, and seal.

V3.7 has one fresh namespace and one nonretryable acquisition attempt; this
does not require one process invocation. Clean continuation is allowed only
between fully sealed v3.7 roles after an authenticated clean close. An open
intent, unsealed response, failed role, terminal rejection, or crash debris can
never be adopted, resealed, resent, or resumed. No private v3.6 namespace may
be mounted, read, or referenced by a v3.7 production or test path.

The production entrypoint remains development-only; intermediate and final
calls reject. The modules expose no Yahoo, Gemma generation,
scientific-feature, portfolio/performance-ledger, broker, or trading path; the
frozen SEC dispatch ledger remains unchanged. A passing parser role is not a
source seal. After a complete development source pass, the generated single
`e/aapl_sec_gemma_lean_evidence_v3_7/development/source-authority-<sha256>.json`,
the redacted `DEVELOPMENT_ACQUISITION.json`, and the comparison update must be
committed and pushed without implementation or scientific-code changes. A
separate read-only gate must authenticate that pushed preservation commit,
allowlisted evidence-only delta, public authority, checkpoint, terminal pass,
and detached replay before inherited development science can open. A rejection
cannot yield authenticated source authority and is preserved instead.

## Required offline tests before implementation publication

Focused tests must prove at least:

- both frozen calendar validators, compact identities, counts, bounds, hashes,
  and exact ordered 504-session suffix relation;
- caller input remains exactly the 2000-onward experiment tuple, bundle replay
  stores that tuple, and caller history/subset/superset/reordering rejects;
- a far-prehistory boundary and `1997-12-30` are censored to null availability
  and null stage, while `1997-12-31` maps exactly to `1998-01-02`;
- 1998 and 1999 weekday, weekend, and holiday boundaries select the exact next
  frozen history session without entering a stage;
- `1999-12-30` maps to `1999-12-31` outside the experiment and
  `1999-12-31` maps to `2000-01-03` inside development;
- the maximum of filing, acceptance, and change date remains the only boundary,
  including an old filing whose authenticated later change enters a stage;
- the inherited synthetic development `I=128`, `P=128`, request formula 245,
  and exact source set remain unchanged while the one old ordinary row leaves
  `U/D`, changing synthetic `D` from 73 to 72;
- development projection count exactly equals `D`, remains at most the frozen
  model-call cap 80, and any synthetic `D=81` still rejects;
- intermediate `D=19`, final `D=12`, completed-year coverage, full/compact
  stage seals, checkpoints, carried prefixes, and detached replay remain exact;
- a null/pre-stage row cannot become a science document, lesson, label, Brier
  observation, readiness count, model request, action difference, or ledger
  input;
- dropping, adding, swapping, duplicating, or mutating either calendar date,
  identity, hash, count, offset, role, censor rule, suffix relation, or
  authoritative composite hash rejects;
- a rehashed forged composite receipt still rejects against the exact
  module-import-authenticated authority;
- all v3.6 and earlier source, acceptance, form, envelope, byte, topology,
  missing-filename, cap, journal, privacy, transport, crash, timeout,
  checkpoint, replay, and compatibility tests remain valid after mechanical
  namespace derivation;
- a hostile cap raise, sampler, truncation, row special case, sentinel clamp,
  broad caller, current-only search, unordered suffix proof, boundary
  weakening, stage-window change, replay mismatch, shared-file edit, non-AST
  byte change, or cross-anchor mutation fails the committed-blob proof; and
- the fresh two-call preflight cannot call version, chat, generation, SEC,
  Yahoo, market, performance, broker, or trade paths.

Neither tests nor implementation may open or parse a saved v3.6 response or
any predecessor private artifact. Synthetic fixtures establish the general
rule without adapting to unpublished official rows.

## Hard falsifiers and scientific boundary

Before a complete development source seal, v3.7 rejects on any inherited v3.6
source falsifier plus any of these:

- `d_min`, `d_max`, the development model-call cap, a stage window, global
  availability start, request cap, source-set cap, or science rule changes;
- a filing is sampled, trimmed, dropped, reordered, reclassified by identity,
  or selected through the observed `D=97`, a date, accession, form count,
  response hash, ordinal, or other predecessor-specific property;
- the caller may supply anything except exact `EXPECTED_SESSIONS`, or bundle
  replay stores/accepts the history tuple as caller authority;
- availability does not use the unchanged maximum source-date boundary and
  strict first frozen history session `>` that boundary;
- unsupported prehistory is assigned a false exact 1998 availability, or a
  genuine 1998/1999 boundary is collapsed, clamped, or promoted into
  development;
- a pre-stage/null row enters `U`, `D`, science projection, model calls,
  lessons, labels, controls, performance, or authorization;
- the two calendar identities, full ordered suffix, prefix count, compact
  hashes, roles, derived censor threshold, or composite receipt are weakened;
- the shared calendar, science contract, parser, model, feature, market,
  learner, ledger, performance, or authorization code changes;
- seal creation and seal validation/replay do not independently derive the
  identical calendar/censor result;
- any predecessor private byte, receipt, journal, manifest, parse receipt,
  checkpoint, source authority, or probe marker is read or reused;
- the preregistration is not one document-only child of the exact v3.6
  terminal commit, or the implementation is not one exact twelve-addition
  child of the preregistration;
- any implementation change falls outside the exact twelve-path committed-blob
  and changed-symbol budget;
- request effects are not reported as exact v3.7 counts plus the inherited 566
  family requests, or any request, byte, time, role, or intent cap is exceeded;
- the preflight is rerun after a durable identity intent, a role is retried,
  an unsealed response is adopted, or a terminal attempt is resumed; or
- any unplanned source, model, market, performance, paid-API, broker, or
  trading effect occurs.

If fresh official v3.7 data still produces `D < 72`, `D > 80`, or another
source failure, v3.7 is terminally rejected and preserved. It is not repaired
during the attempt. Any successor requires another separately committed and
pushed preregistration based only on public source evidence, with science and
later stages still closed.

If the development source seal passes, source evidence is committed and
pushed first, then authenticated by a separate read-only gate. Only after that
gate may inherited development science run without modification. Confirmation
and final data remain closed until their unchanged gates pass. No historical
result can authorize real capital. Any historically promising survivor must
make append-only prospective paper-trading decisions before outcomes are
known; real-money trading remains outside this goal without fresh explicit
user authorization.
