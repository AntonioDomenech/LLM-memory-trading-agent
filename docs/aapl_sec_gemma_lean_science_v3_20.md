# AAPL SEC/Gemma lean science v3.20 preregistration

## Status and zero-effect boundary

This document is the complete immutable specification for one fresh
development-only AAPL-or-cash successor attempt. Creating and pushing this
document performs no qualification, preflight reservation, source replay, SEC
request, Yahoo request, Ollama request, Gemma generation, market-value read,
prediction, action, return or performance calculation, confirmation or
live-style data open, paid API call, broker action, or real-money action.

V3.20 is a process-only successor to the V3.19 exceptional zero-effect
rejection. V3.19 passed its official preflight and authenticated its complete
75-request plan. Its official development process then stopped at the first
`attempt_open` runtime checkpoint, before any external intent, because source
authentication lazily loaded the Python standard-library `_strptime` module
after the dependency baseline had been sealed. The production recovery command
could not preserve normal `R319` evidence because its one-use recovery proof
was routed through the development loader first and its recovery authority
rejected the exact candidate-less zero-effect state it was designed to seal.

V3.20 changes no source, filing cohort, sanitizer, request, model, prompt,
market series, feature, learner, policy, threshold, transaction cost,
chronological stage, scientific gate, or effect budget. It makes exactly three
process corrections:

1. deterministically preload `_strptime` before runtime dependency capture;
2. route publication recovery directly to its own authority loader without
   making the launch proof reusable; and
3. allow that loader to authenticate only the exact replay-proven
   zero-intent `rejected/runtime_binding_mismatch` store state.

## Immutable base and predecessor authority

The V3.20 branch is
`codex/aapl-sec-gemma-lean-science-v3-20`. Its immutable successor base is the
pushed V3.19 exceptional-preservation commit
`1e1f323fa6b50797874d8b0fff7ad8ae992cb128`, tree
`3ec2fce9596506cee09945287c5bb7397ac71e05`, whose sole parent is passing
`F319` commit `dcdb4a78beb1bb954701ed012c829d5040918166`, tree
`e1fafd365341abdf72d15add68553ffab9bec26b`.

The V3.19 preregistration is Git blob
`bb206dbeafe958b42e35dcfcc5798a2e4154b4f1`, 24,009 UTF-8 bytes, with literal
SHA-256
`5ff7f4070008d2dbb5dce40e48e309282803f7ef40c0b62125eebe5dd30e5fe7`.
The V3.19 exceptional rejection is Git blob
`e8dbec01b538889258b110314d2725ae0ce32a20`, 6,604 UTF-8 bytes, with literal
SHA-256
`1a1fc56081ffd8562d32fac70c99755669268eee65c4400870312c94a6d793eb`.
The passing V3.19 public preflight artifact is Git blob
`2aa77b3b8b8f9cf5d70f24ac936882d3213c4b80`, 9,014 bytes, with literal
SHA-256
`3f7e228458c88f3dd9f9fb9d3c7a9e793fb0b950219583ca054ac23a090e31c4`.
At the successor base, `e/APPROACH_COMPARISON.md` is Git blob
`8fedebde704929d07c97c2b1cc6973aadfe98a96`, 38,226 UTF-8 bytes, with literal
SHA-256
`3d45e82eca4852f098715bf4c2f38613b1bfb94a6afd152365fbf154d28286c8`.

V3.19 has no terminal result, performance metric, strategy verdict, or later
stage authority. Its private attempt is consumed and must never be retried,
reused, copied, or interpreted as V3.20 state. V3.20 uses a fresh versioned
namespace, attempt identity, runtime authority, one-shot preflight, and Git
topology.

The V3.20 document-only preregistration commit is named `P320`. It must have
the V3.19 exceptional-preservation commit as its sole parent, add only this
document, and be pushed and live-remote authenticated before any V3.20
implementation path is created. Once pushed, `P320` is immutable.

## Exact inheritance and replacements

V3.20 incorporates every normative V3.19 preregistration provision after the
exact eight-form mechanical mapping `V3.19 -> V3.20`, `v3.19 -> v3.20`,
`V3_19 -> V3_20`, `v3_19 -> v3_20`, `V3-19 -> V3-20`,
`v3-19 -> v3-20`, `V319 -> V320`, and `v319 -> v320`, plus the topology
mapping `P319/I319/F319/R319/S319/C319/X319 ->
P320/I320/F320/R320/S320/C320/X320`, except for the explicit replacements in
this document.

The mapping applies to branch, module, class, function, schema, namespace,
attempt, artifact, pending suffix, phase, command, safe-code, comparison-row,
and Git-topology identities. It never rewrites quoted V3.8 through V3.19
commits, trees, blobs, hashes, byte counts, observed failure facts, or the two
exact case-distinct Phase-2 node IDs.

Only these provisions are replaced:

1. successor base, branch, ancestry, preregistration, implementation,
   namespace, attempt, artifact, schema, and topology identities;
2. the six production and six test path names;
3. the Phase-1 selector paths, minimum node count, and required-node list;
4. runtime repository preloading and revision-owned runtime manifests, by the
   exact `_strptime` correction below;
5. the production `DevelopmentDependencies` recovery authority boundary and
   recovery dispatch below; and
6. candidate-less publication-recovery authentication for the exact
   zero-effect binding rejection below.

There are no other replacements. In particular, the V3.19 direction
sanitizer and shared final predicate, exact seven-request/eight-sentence
aggregate, source bytes, nullable-source logic, 75 canonical requests, prompt,
fixed `gemma4:12b` model, context, output schema, Yahoo series, features,
learner, policy, thresholds, transaction costs, development gates,
confirmation closure, effect budgets, qualification environment, scientific
bootstrap, literal repository preload, runtime-v2 identities, JUnit parity,
Requests sentinel, publication durability, terminal construction, and
pushed-result authentication remain unchanged except where this document
explicitly changes process routing.

## Exact deterministic `_strptime` preload

The V3.20 `_runtime_preload_repository_modules` function must import the
standard-library module named exactly `_strptime` through one literal,
allowlisted import before it returns and before either
`_build_execution_dependency_manifest` or `_build_loaded_code_manifest` can
run. This same function is already called by both preflight runtime-authority
creation and development/recovery runtime-session initialization, so no second
preload implementation is allowed.

The import must be fail-closed under the existing runtime-preload error family.
It must require the returned object to be the exact `sys.modules['_strptime']`
module, require `__name__ == '_strptime'`, and allow the existing dependency
manifest builder to bind its resolved source path, byte count, and literal
hash. It may perform only the normal Python import-loader reads needed to load
that exact standard-library source module and the inherited manifest identity
reads. It must not call `datetime.strptime`, read private source, inspect a
filing, access a clock value, or perform any other filesystem, Git, network,
SEC, Yahoo, Ollama, model, market, broker, or real-money work. It must not add
`_strptime` to the repository-module allowlist because it is a
standard-library source module.

The predecessor read-only proof established the causal expectation:

- dependency module-file count changes from 365 to 366;
- the sole added predecessor-runtime row has module name `_strptime`, origin
  kind `stdlib_source`, byte count 25,180, and literal SHA-256
  `8961aa3bf0fe6d677d26f83ea25c72685a6c7dde51d3bbd2ec4a2a405676a320`;
- the 44 repository module rows and 3,242 protected callable rows do not change
  during source, projection, commitment, or plan construction; and
- representative later `datetime.strptime` calls do not change either
  manifest once `_strptime` is preloaded.

V3.20 must derive and bind its own exact manifest hashes from pushed I320 bytes;
it may not copy V3.19 manifest hashes. Offline production-order tests must
prove repository, dependency, and loaded-code equality immediately before and
after the actual preserved-source authentication, streaming projection,
request commitments, and model-plan build. Any new module row or callable
identity appearing after the V3.20 baseline is a hard failure.

## Exact publication-recovery authority routing

`DevelopmentDependencies` gains one required callable named
`authenticate_publication_recovery` with the exact logical signature
`Callable[[Path], Mapping[str, Any]]`. Production binds it directly to
`load_publication_recovery_authority`.

The existing `authenticate_execution(root, descendant)` boundary becomes
strictly execution-only:

- `descendant is False` authenticates only the separately pushed passing F320
  and loads its normal development authority;
- `descendant is True` loads only an allowed pause/continuation/result
  descendant execution authority; and
- neither route catches an arbitrary exception and reinterprets it as
  publication recovery.

`recover_publication` must call
`authenticate_publication_recovery(root)` directly. It must never call
`authenticate_execution` first and must never reach recovery through a broad
exception fallback. Development and continuation must never call the recovery
callable.

`_consume_runtime_launch_proof` remains one-use and fail-closed. Its deletion,
self-hash, environment, bootstrap, path, and invocation checks remain
unchanged. A `publication_recovery` proof must reach exactly one recovery
session initializer and be removed exactly once. It must never be accepted as
a `development` or `continuation` proof, and a missing, reused, wrong-route,
wrong-root, wrong-bootstrap, wrong-environment, or wrong-path proof must still
reject.

The unreachable production `status` command is not changed or added to the
scientific bootstrap in V3.20. Status routing is unnecessary for the exact
development and publication-recovery correction and would enlarge the route
and authority surface.

## Exact zero-effect recovery authority

When `load_publication_recovery_authority` opens a replay-validated store with
no committed terminal receipt, it retains the inherited paused-state branch
and adds exactly one mutually exclusive branch for a candidate-less runtime
binding rejection. The new branch is valid only when every condition below is
true:

1. the authorized Git parent is exactly the pushed preflight, never pause,
   continuation, result, merge, or another descendant;
2. the replayed snapshot status is `rejected`, terminal code is
   `runtime_binding_mismatch`, and its journal head is the store's exact
   replay-validated `runtime_binding_failure` event;
3. that event is checkpoint `attempt_open`, has failure code
   `runtime_binding_mismatch`, has `external_intent_count == 0`, has no prior
   runtime-binding receipt, and directly follows the attempt-intent event;
4. event count is exactly two; request, Yahoo, identity, Gemma, response,
   checkpoint, response-body, and segment counts are all zero;
5. active request, pending checkpoint, and open model segment are all absent;
6. pause-required, paused, continuation-authorized, market-values-opened, and
   model-responses-opened are all false; continuation hashes/commit are null;
7. pause candidate is absent, and the store contains no effect-bearing state;
8. the result, pause, result-pending, pause-pending, and comparison-pending
   public paths are absent, the comparison working bytes equal the committed
   HEAD bytes, and tracked/staged/untracked state is otherwise exactly clean;
   and
9. all repository, preflight, attempt-authority, runtime, and public-path checks
   already performed by the recovery authority loader pass exactly, and the
   inherited private-contact, source, projection, request-commitment,
   model-plan, and privacy checks performed by `recover_publication` before its
   first mutation also pass exactly.

The loader is read-only: before and after public-path state must be identical,
and opening/closing the store may not append or alter a byte. A replay-valid
private recovery intent or terminal candidate created by an interrupted
zero-effect recovery is allowed only as already permitted by the inherited
store state machine. Before candidate persistence, recovery may recreate and
seal the exact zero-effect rejection. A candidate persisted without an
attempt-terminal binding remains untrusted and follows the inherited
`indeterminate/unbound_terminal_candidate` demotion; V3.20 must not rebind it.
Once an attempt-terminal receipt exists, inherited bound-terminal recovery may
finish the exact publication idempotently. This authority never authorizes a
new development attempt or any external effect.

Any one-field deviation rejects under a fixed redacted
`publication_recovery_*_invalid` code. In particular, one external intent,
response, payload/body byte, market/model open, continuation parent, public or
pending artifact outside the exact recovery state, dirty comparison, altered
journal link, non-`attempt_open` checkpoint, other terminal code, or reusable
launch proof must fail closed.

The inherited V3.20 store logic for `zero_effect_binding_rejection`, recovery
intent, recovery runtime receipt, terminal candidate persistence, attempt
terminal sealing, idempotent public publication, recovery terminal, and crash
replay is unchanged. No alternate hand-authored result path is allowed.

## Implementation topology

`I320` has `P320` as its sole parent and adds exactly these twelve paths:

```text
agent_benchmark/sec_gemma_lean_science_v320_contract.py
agent_benchmark/sec_gemma_lean_science_v320_bridge.py
agent_benchmark/sec_gemma_lean_science_v320_journal.py
agent_benchmark/sec_gemma_lean_science_v320_store.py
agent_benchmark/sec_gemma_lean_science_v320_preflight.py
agent_benchmark/sec_gemma_lean_science_v320_runner.py
tests/test_sec_gemma_lean_science_v320_contract.py
tests/test_sec_gemma_lean_science_v320_bridge.py
tests/test_sec_gemma_lean_science_v320_journal.py
tests/test_sec_gemma_lean_science_v320_store.py
tests/test_sec_gemma_lean_science_v320_preflight.py
tests/test_sec_gemma_lean_science_v320_runner.py
```

No tracked predecessor path may change in `I320`. The twelve V3.20 files must
be mechanically derived from the exact committed V3.19 implementation and
then receive only this document's three process corrections and their direct
tests. They may not import or execute a V3.19 or older production/test module
or historical strategy entry point. The inherited 37-module shared closure,
package initializer, standard library, and exact qualified third-party runtime
remain the only allowed imports outside the six successor production modules.

## Required direct tests and qualification

Every mechanically mapped V3.19 test remains mandatory. Phase 1 runs every
collected node in the six V3.20 test files, must collect at least 341
case-sensitive unique nodes with zero duplicates, and must pass every node. It
includes all 32 mapped V3.19 required nodes plus exactly these eight required
nodes:

33. `tests/test_sec_gemma_lean_science_v320_preflight.py::test_runtime_preload_includes_strptime_before_both_dependency_captures`
34. `tests/test_sec_gemma_lean_science_v320_preflight.py::test_runtime_binding_stays_exact_after_source_auth_datetime_parse`
35. `tests/test_sec_gemma_lean_science_v320_runner.py::test_production_recovery_routes_directly_to_recovery_authority`
36. `tests/test_sec_gemma_lean_science_v320_preflight.py::test_publication_recovery_launch_proof_is_consumed_once_by_its_direct_route`
37. `tests/test_sec_gemma_lean_science_v320_preflight.py::test_production_recovery_authority_accepts_exact_zero_effect_binding_rejection_read_only`
38. `tests/test_sec_gemma_lean_science_v320_preflight.py::test_production_recovery_authority_rejects_effect_and_public_state_mutations`
39. `tests/test_sec_gemma_lean_science_v320_runner.py::test_production_zero_effect_binding_rejection_recovers_idempotently_without_external_effects`
40. `tests/test_sec_gemma_lean_science_v320_runner.py::test_zero_effect_recovery_preserves_safe_outcome_at_each_private_and_public_crash_boundary`

The preload tests must use the real runtime manifest builders and prove the
exact first-divergence cause is absent. The routing test must prove recovery
calls only the dedicated loader while development and continuation never call
it. The proof test must use the real one-use sentinel and reject replay. The
authority tests must use an actual AttemptStore and real public-path inspection,
not a snapshot-only fake. All eight named required nodes must remain literal,
unparametrized node IDs; mutation cases inside required node 38 must loop
internally, while additional non-required tests may be parametrized. The
cross-module recovery test must create the exact first-checkpoint failure
through production-composed dependencies, prove zero Yahoo/Ollama/Gemma calls,
publish the exact rejected artifact, rerun recovery, and prove no additional
attempt event, recovery event, or public write. Crash coverage must prove the
exact inherited safe outcome at each boundary: retry and seal the rejection
before candidate persistence; demote an unbound persisted candidate to
`indeterminate/unbound_terminal_candidate`; resume bound publication after
attempt-terminal sealing; and remain idempotent across each partial public
publication boundary.

Phase 2 remains the exact 14 ordered shared selectors: 655 ordered nodes, 655
case-sensitive unique IDs, zero duplicates, and ordered node-list SHA-256
`1de8cc183af68c089aa5616f5e51405abda34382ff15e73123f5cd3c0dfea83c`.
Phase 3 remains the one exact Requests-identity sentinel. The controlled
environment, deadlines, exact collection/execution parity, zero
skip/xfail/xpass policy, and once-only reservation remain unchanged.

Before I320 is pushed and again immediately before official preflight is
reserved, the inherited redacted all-request proof must authenticate the
preserved V3.8 source and report exactly 75 requests, seven requests with
removals, eight removed sentences, zero empty required partitions, zero V3.20
contract failures, zero inherited-production failures, and zero external
effects. It must also report the preload/runtime equality aggregate without
private paths or module source contents. Any other aggregate blocks I320 or
official preflight.

## Chronology, effects, and terminal topology

Development remains 2000-2018, confirmation remains 2019-2023, and live-style
evaluation remains 2024 onward. Confirmation and live-style data stay closed
unless all 22 inherited development gates pass and the separately pushed
result gate authenticates the exact result. Buy-and-hold remains evaluated at
both 5 bps and 10 bps. No shorting, leverage, borrowing, negative cash, paid
API, broker action, or real-money action is authorized.

Official preflight is once-only and zero-effect. `F320` adds only its public
preflight artifact. `R320` adds only an authenticated terminal result and
changes the comparison table. `S320` adds only a pause artifact. `C320` adds
only a continuation document. A consumed preflight failure uses mapped X320
topology. All failures and successful results are committed and pushed as
immutable evidence.

If development fails any frozen gate, V3.20 is permanently rejected and no
later stage opens. If development passes, the pushed-result gate must
authenticate exact private/public evidence before confirmation can be
considered. Nothing in this preregistration authorizes real-money trading.
