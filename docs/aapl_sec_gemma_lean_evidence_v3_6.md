# AAPL SEC/Gemma lean evidence v3.6

## Status and immutable predecessor

This is a new, separately preregistered acceptance-time successor on
`codex/aapl-sec-gemma-lean-evidence-v3-6`. This document must be committed and
pushed by itself before any v3.6 implementation commit, programmatic SEC
request, Yahoo value, Gemma generation, performance calculation, confirmation
source, or final source is opened.

V3.5 is terminally rejected and preserved at commit
`d1fec255a5ae6a77a047906683fc3e1f50f49298`, tree
`d85a6a79ac2f198ce50daf3aaeb25fbe8ff8b646`. Its public terminal artifact is
`e/aapl_sec_gemma_lean_evidence_v3_5/DEVELOPMENT_ACQUISITION.json`, Git blob
`3bcb42cc8181b8f23612317e611755c35c6042e3`, literal file SHA-256
`550b83cb178bb30380ca6ab4a068ab8cb6820fbe03cd1905043d6cd2bc15bcb6`, and
internal acquisition SHA-256
`811bfe18eb44e14bf14b7984db25f8f2f0d00c59091c0d3113b6d41adf60c336`.
V3.6 never resumes, repairs in place, or reclassifies that attempt.

The v3.5 preregistration is pinned at commit
`50d7e21a0171edf0f356622c2160f996296fd503`, tree
`176ce7b59d89513a74078d487c0ea15ea40e64d6`, document blob
`92c4d0773dbd2b4338a1d00797861927480b4eab`, and document SHA-256
`bdf2c07db01717cb2cf4eb4fe002e20a93d4ac0aa2e1a5b3534da8ff22259bf1`.
Its implementation is pinned at commit
`22900806967c8d8d93fd37bdf91175c413ee3284`, tree
`b0ca79306a01ec092e073e21a015b3021fc7de2a`, with committed-blob delta
manifest SHA-256
`d66056b8728a8dff7ef50371486fc0777ea97c2babab8d6405c7139924447e07`.
Its zero-effect preflight is pinned at evidence commit
`48f2b1d3b82674f1bc252d81e9c436f895b6d156`, tree
`d21dc6d3715143565a5c4dea0843dc1827da7742`, public artifact blob
`34cce9ff487a1c5bc05a08ddabad6e4bf313a942`, preflight SHA-256
`40a7a4d01b4f7a67195cbdcb1495c36e89a69e9dc3fb4269273c53084d7fb986`, and
literal file SHA-256
`e65471e545afa538bbedebf3bff23c12e1058aa11539af9549d6ec0a246b299f`.
The executed v3.5 production-source inventory SHA-256 is
`518d95c35f69e7c1fe2d358304fabe795697ef461ef0863cc175725ad9e6ed89`.
V3.5 has no checkpoint, complete-source detached replay, public source
authority, or resumable attempt. The original scientific parent remains
`efbc481c57e480d48303763163676e64e87df49d`.

## Evidence already seen and evidence still closed

V3.5 made exactly 134 fresh official SEC requests, all of which returned HTTP
200. It sealed 133 roles: the main Apple Submissions response, one historical
Apple Submissions response, every quarterly master from 1994-Q1 through
2018-Q4, and the first 31 complete-submission roles. The 32nd
complete-submission role, `complete/0000912057-02-030796`, then received
488,955 bytes with body SHA-256
`19cfa0f30f63033db76282d135bc0b6ed7073f7f95f8a509944b1fae2b8511a2`
but returned terminal `parse_rejected` before its parse receipt, manifest, role
seal, checkpoint, complete-source detached replay, or source authority.

The untouched failed body passes the frozen shared complete-submission parser,
the inherited all-document topology gate, and the v3.5 family-neutral raw-form
gate. It is the first v3.5 complete body having both a Submissions acceptance
value and an SGML-header acceptance value. The first failing gate reports
`Acceptance values do not reconcile lexically`.

The inherited helper interprets both representations as New York wall-clock
labels. A read-only source-only diagnostic instead interpreted the exact
uppercase-`Z` Submissions representation as a UTC instant and converted it
through `America/New_York`; that result reconciled exactly with the strict
14-digit SGML wall-clock label, and the untouched body then passed full source
reconciliation. This is a diagnostic hypothesis only. It is not source
authority, cannot authorize later stages, and cannot be read or reused by
v3.6 production.

Every v3 through v3.5 SEC byte, receipt, manifest, journal event, seal,
checkpoint, typed row, probe marker, or diagnostic is predecessor evidence
only. V3.6 must reacquire every role into fresh namespaces. No predecessor
artifact can satisfy a v3.6 request, seal, exact-set reconciliation, checkpoint,
or authority. Production code may not read a predecessor private namespace,
rewrite a predecessor body, or compare predecessor and v3.6 bodies to select
behavior.

The 134 role identities opened by v3.5 are substantively repeated source
evidence when freshly reacquired. V3.6 does not start at the failed role or
assume v3.5's historical-reference count, target set, ordering, or planned
request formula. `H`, `I`, `P`, and the exact target set must be rebuilt from
fresh v3.6 responses. Before a complete v3.6 development source seal, all
Yahoo values, Gemma outputs, feature rows, labels, ledgers, performance
results, confirmation sources, and final sources remain closed.

Before v3.6, this approach family has made exactly 367 official SEC requests:
v3 made 4, v3.1 made 2, v3.2 made 15, v3.3 made 103, v3.4 made 109, and v3.5
made 134. Every v3.6 public preservation must report v3.6 effects separately
and the exact cumulative family total as
`367 + v3.6 actual official SEC requests`. Every durable v3.6 SEC role intent
counts once, including a request that fails or produces no usable response. A
freshly reacquired repeated role is still a new v3.6 request. The two permitted
loopback identity calls are not SEC requests and must be reported separately.

## Frozen hypothesis and unchanged science

The scientific question remains literal and unchanged:

> Can a fixed local Gemma reader extract point-in-time deterioration evidence
> from Apple 10-K and 10-Q filings that helps a chronological long-or-cash
> learner avoid enough harmful AAPL exposure to add after-cost value over the
> frozen exhaustion baseline and a no-filing-meaning control?

V3.6 inherits every scientific rule from the pinned predecessor documents:

- model identity, model fingerprint, prompt, schema, seed, temperature,
  context, and output caps;
- the twelve filing values, market features, and missingness flags;
- learner, fitting, threshold, interval, non-overlap, and next-open rules;
- inherited exhaustion baseline, semantic control, and no-meaning control;
- development, confirmation, final, and frozen-learning gates;
- 5-bps and 10-bps same-ledger AAPL buy-and-hold comparisons; and
- AAPL-or-cash only, no shorting, no leverage, no borrowing, no negative cash,
  no paid API, no real-money action, and no future leakage.

`H`, `I`, `P`, `Q`, `U`, `D`, every cap, the 95% acceptance requirement,
admitted counts, chronological windows, stage ordering, and every performance
threshold remain unchanged. No filing deletion, observed-body exception,
prompt rewrite, feature change, class rebalance, model swap, threshold search,
alternate horizon, or performance-informed repair is allowed.

The only new source-format hypothesis is:

> A strict 14-digit acceptance value is a New York wall-clock label. It remains
> on the inherited `America/New_York` localization path, including rejection of
> nonexistent spring-gap and ambiguous autumn-fold wall clocks. An exact
> uppercase-`Z` value is instead a UTC instant. It must have no fractional
> suffix or a one-to-nine-digit all-zero fractional suffix, be parsed as UTC,
> and then be converted through `America/New_York` timezone rules. The two
> representations reconcile only when their resulting Eastern
> `YYYYMMDDHHMMSS` values match exactly.

This is a falsifiable acceptance-time representation hypothesis, not proof
that the remaining development filings will parse. UTC instants are
unambiguous and do not undergo the local gap/fold rejection applied to strict
local labels. Existing malformed-value, exact-mismatch, one-source-missing,
both-sources-missing, filing-date, availability, hash, and replay behavior
remains unchanged.

## The only source-format repair

The shared helpers and tests in `agent_benchmark/sec_point_in_time.py`,
`tests/test_sec_point_in_time.py`, `agent_benchmark/sec_filing_content.py`, and
`tests/test_sec_filing_content.py` are frozen exactly at the v3.5 terminal
base. Their historical public behavior is not silently changed for predecessor
approaches. None of these shared paths may appear in the v3.6 implementation
delta.

The mechanically cloned v3.6 source adapter makes one local, deliberately
bounded repair:

1. Alias the inherited shared import as
   `_parse_submissions_acceptance_wall_clock`.
2. Add `timezone` to the existing `datetime` import and add exactly
   `from zoneinfo import ZoneInfo`.
3. Define one undecorated local function with the existing public module name:
   `parse_submissions_acceptance_datetime`. Existing v3.6 call sites remain
   byte-mechanically unchanged and therefore use this wrapper.
4. Reject a non-string value. Delegate an exact `[0-9]{14}` value to
   `_parse_submissions_acceptance_wall_clock`, retaining the inherited
   calendar validation and New York spring-gap/autumn-fold rejection.
5. Admit as the UTC branch only this exact uppercase grammar:
   `[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}` followed by either
   no fraction or `.` plus one through nine zero digits, then one terminal `Z`.
6. Parse the date and time as a UTC instant using `datetime.strptime` and
   `timezone.utc`, then convert it with
   `.astimezone(ZoneInfo("America/New_York"))`. No fixed offset, local-digit
   reinterpretation, rounding, truncation, or local gap/fold validation of the
   UTC spelling is permitted. Any invalid-calendar/time `ValueError` must be
   re-raised as `SecPointInTimeError` so every existing caller continues to
   fail closed through the inherited exception contract.
7. Delegate every spelling outside the admitted UTC grammar to the frozen
   shared helper so the inherited exact rejection surface remains closed.
8. Preserve both original raw spellings in typed evidence, compact evidence,
   checkpoints, and detached replay. Only the derived Eastern
   `YYYYMMDDHHMMSS` value is compared. No response byte, filing text, offset,
   hash, identity, or availability input is rewritten.

The UTC branch must reject lowercase `t` or `z`, an explicit offset such as
`+00:00`, surrounding or embedded whitespace, missing seconds, an empty
fraction, a fraction longer than nine digits, any nonzero fractional digit,
invalid calendar or clock fields, a leap second, or any suffix. A malformed
value on one side cannot fall back to a valid value on the other side. Exact
one-second disagreement after conversion still rejects. The inherited
one-source-missing and both-sources-missing paths remain unchanged.

The repair is representation-general. Production and tests must not contain,
copy, or branch on the failed v3.5 accession, filing date, the failed response's
literal acceptance timestamp values, observed offset difference, role/request
ordinal, response length/hash, header hash, document count/topology, filename
state, or any other unique predecessor property. They must not read the saved
body or a fixture derived from it. A fixed `-04:00` or `-05:00` adjustment,
observed-value lookup, exception table, retry, or body transformation is
forbidden.

## Frozen stage configurations and caps

No stage, request, byte, or time bound changes from v3.5:

| Stage | Availability window | Master coverage | `Q` | `I` cap | `P` cap | Admitted `D` | Maximum requests |
|---|---|---:|---:|---:|---:|---:|---:|
| Development | 2000-01-01 through 2018-12-31 | 1994 Q1 through 2018 Q4 | 100 | 128 | 128 | 72-80 | 245 |
| Intermediate | 2019-01-01 through 2023-12-31 | 1994 Q1 through 2023 Q4 | 120 | 160 | 24 | 19-20 | 161 |
| Final | 2024-01-01 through 2026-07-09 | 1994 Q1 through 2026 Q3, filtered at the cutoff | 131 | 176 | 16 | at most 12 plus every inherited completed-year rule | 164 |

A passing stage still has exactly `1 + H + Q + P` successful official SEC
requests. `H <= 16`; lifetime intents remain capped at 256 per stage. The
individual, aggregate successful-response, aggregate decompressed-master,
lifetime received-byte, `L + 1` reservation, TLS, privacy, timeout,
pause/resume, one-intent, no-retry, and crash-open rules remain exactly those
of v3.5. The fixed final cutoff does not advance with the calendar. V3.5's
planned development formula of 199 is evidence about v3.5 only and is not a
hard-coded v3.6 request count.

For development, the unchanged individual response limits are 32 MiB for the
main Submissions response, 64 MiB for a historical Submissions response,
16 MiB compressed and 128 MiB decompressed for a master, and 128 MiB for a
complete submission. The unchanged aggregate caps are 19,964,887,040
successful-response bytes, 21,474,836,480 lifetime received bytes, and
17,582,522,368 decompressed-master bytes. The first dispatch wait is 1,000 ms,
the global dispatch gap is 500 ms, request deadline is 30,000 ms, role deadline
is 600,000 ms, invocation deadline is 14,400,000 ms, and cumulative active
development time is capped at 43,200,000 ms.

## Canonical v3.6 sequence

After all Git and preflight gates below pass, development runs in this order:

1. reacquire the main Apple Submissions JSON once;
2. reacquire every referenced historical Submissions file once in canonical
   filename order;
3. build rejection-only pre-master `I` and `P` upper bounds;
4. fetch every master from 1994-Q1 through 2018-Q4 in canonical order;
5. retain every structurally valid row under the unchanged v3.5 master rule;
6. exact-set reconcile the complete Apple Submissions/master target set;
7. fetch every required complete submission in `(filing_date, accession)`
   order from the exact master filename;
8. parse each original body under the unchanged exclusive SEC-or-IMS envelope
   and family-neutral raw-form rules, then reconcile acceptance sources through
   the new representation-aware timestamp rule without rewriting either source;
9. finalize the complete development source seal and exact detached replay;
10. only then allow inherited development Yahoo values, fixed Gemma requests,
    features, labels, ledgers, or performance calculations.

Intermediate remains forbidden unless development passes its unchanged
scientific gate. Final remains forbidden unless intermediate passes. V3.6
reacquires its own response-time evidence; all predecessor bytes remain
diagnostic source-only evidence.

## Implementation and committed-blob proof

The mandatory sequence is:

1. commit and push this document alone;
2. add the exact v3.6 implementation and focused offline tests;
3. commit and push that implementation;
4. run one fresh zero-source-effect v3.6 preflight;
5. commit and push the preflight receipt alone;
6. only then make the first v3.6 SEC request;
7. preserve and push either the terminal rejection or complete source seal
   before any scientific stage can open.

The implementation commit may add exactly these twelve regular `100644`
files:

- `agent_benchmark/sec_gemma_lean_v36_acquisition.py`;
- `agent_benchmark/sec_gemma_lean_v36_delta.py`;
- `agent_benchmark/sec_gemma_lean_v36_journal.py`;
- `agent_benchmark/sec_gemma_lean_v36_preflight.py`;
- `agent_benchmark/sec_gemma_lean_v36_source.py`;
- `agent_benchmark/sec_gemma_lean_v36_transport.py`;
- `tests/test_sec_gemma_lean_v36_acquisition.py`;
- `tests/test_sec_gemma_lean_v36_delta.py`;
- `tests/test_sec_gemma_lean_v36_journal.py`;
- `tests/test_sec_gemma_lean_v36_preflight.py`;
- `tests/test_sec_gemma_lean_v36_source.py`; and
- `tests/test_sec_gemma_lean_v36_transport.py`.

No existing tracked path may change. Relative to the preregistration commit,
the implementation delta must therefore contain exactly twelve `A` records
and zero `M` records, with no rename, copy, deletion, type change, submodule,
or nonregular mode.

Each added file must be derived from its exact committed v3.5 counterpart at
the pinned terminal commit. The ordered mechanical namespace replacements are
`sec_gemma_lean_v35` to `sec_gemma_lean_v36`, `SecGemmaLeanV35` to
`SecGemmaLeanV36`, `V35` to `V36`, `v35` to `v36`, `v3_5` to `v3_6`,
`v3-5` to `v3-6`, and `v3.5` to `v3.6`.

After normalization, the only production changes allowed outside the new
delta verifier are in `agent_benchmark/sec_gemma_lean_v36_source.py`:

- `function|parse_submissions_acceptance_datetime|1`;
- `import|$module.__import__|3` for adding `timezone` to `datetime`;
- `import|$module.__import__|12` for aliasing the shared parser as
  `_parse_submissions_acceptance_wall_clock`; and
- `import|$module.__import__|14` for `from zoneinfo import ZoneInfo`, placed
  after the inherited relative imports so earlier import occurrence identities
  do not shift.

That production file may not change another function, helper, class,
decorator, module assignment, or import. The cloned v3.6 source test may add
exactly these three undecorated functions, with no changed import, helper,
fixture, decorator, or pre-existing test symbol:

- `function|test_acceptance_mismatch_and_missingness_contract_is_unchanged|1`;
- `function|test_acceptance_representation_semantics_are_exact_and_dst_hostile|1`;
  and
- `function|test_utc_acceptance_reconciles_to_eastern_header_across_date_rollover|1`.

The tests must use synthetic non-observed identities and values. Together they
must cover winter and summer conversion, previous-Eastern-date rollover,
strict-label gap/fold rejection, valid UTC instants around the repeated autumn
hour, fraction bounds, the complete malformed matrix, winter and summer full
source reconciliation, exact one-second mismatch, each inherited missingness
state, no malformed-side fallback, filing-date/availability behavior, raw
spellings, hashes, byte offsets, compact serialization, checkpoints, and
detached replay. No test may read a saved v3.5 response or reproduce its unique
identity.

After normalization, acquisition, journal, preflight, and transport plus their
four corresponding test files must have empty changed-symbol and changed-import
sets. No shared parser, shared parser test, model, prompt, feature, learner,
market, ledger, performance, stage, or authorization file may change.

The delta module may change only preregistration/base pins, predecessor
replacement data, exact path/raw/symbol/import/evidence metadata, predecessor
labels, and cross-anchor metadata; the inherited direct-parent and
strict-addition validation remains unchanged, and it may not add a nested
helper. Excluding its self-referential
`assignment|$module._COUNTERPART_RULE_DATA|1`, its admitted normalized
identifiers are limited to
`assignment|$module.PREREG_COMMIT|1`,
`assignment|$module.PREREG_TREE|1`,
`assignment|$module.PREREG_DOC_BLOB|1`,
`assignment|$module.PREREG_DOC_SHA256|1`,
`assignment|$module.IMPLEMENTATION_BASE_COMMIT|1`,
`assignment|$module.IMPLEMENTATION_BASE_TREE|1`,
`assignment|$module._COUNTERPART_MECHANICAL_REPLACEMENTS|1`, removal of
`assignment|$module._V34_CLONE_PATH_RULES|1`, addition of
`assignment|$module._V35_CLONE_PATH_RULES|1`,
and `assignment|$module._DEFAULT_EXISTING_PATH_RULES|1`. The normalized
`_DEFAULT_COUNTERPART_RULES` assignment and `build_delta_manifest` body must
remain unchanged. No checker class, counterpart helper, or other checker
function body may change.

The delta test may change only its exact predecessor/preregistration fixtures,
the excluded `assignment|$module.V36_DELTA_MODULE_SHA256|1`, and one new
hostile test,
`function|test_checker_rejects_fixed_offset_broad_timestamp_scope_and_observed_special_case|1`.
Its admitted predecessor-shift records are
`assignment|$module.EXPECTED_V36_COUNTERPARTS|1`,
`function|_make_counterpart_repo|1`,
`function|test_default_git_pins_are_real_exact_objects_and_prereg_is_doc_only|1`,
`function|test_committed_counterpart_rejects_same_symbol_import_hostile_body|1`,
the decorator of
`test_counterpart_baseline_count_import_and_evidence_tamper_fail_closed`
(`decorator|test_counterpart_baseline_count_import_and_evidence_tamper_fail_closed.__decorator__|1`),
`function|test_counterpart_baseline_count_import_and_evidence_tamper_fail_closed|1`,
and
`function|test_external_anchor_rejects_delta_body_with_stale_test_literal|1`.
The normalized
`test_default_new_file_allow_inventories_match_frozen_worktree_structure` body
and the inherited v3.5 horizontal-whitespace hostile test must remain
mechanically unchanged.

The inherited inventory assertion must bind exactly twelve path rules, twelve
counterpart rules, the exact twelve-path `_V36_IMPLEMENTATION_PATHS` set,
exactly twelve `A` paths, and no `M` path. Every path begins with no prior blob
in the preregistration tree. The expected counterpart table maps every v3.6
path to its exact v3.5 path at the pinned terminal base.

The v3.6 delta checker must operate only on committed Git objects and ignore
all worktree bytes. It must use twelve exact counterpart rules. After the
seven ordered namespace replacements, the source counterpart may differ only
in the one function and three imports listed above, and the source-test
counterpart may differ only by the three new tests. The eight ordinary
non-delta, non-source files must be mechanically identical. The checker must
bind all import inventories, full position-free symbol evidence, raw framing,
replacement counts, and exact changed-symbol sets. Separate publication and
preflight gates reject a dirty worktree and require raw worktree bytes to equal
HEAD. The checker may use only a sanitized environment and the frozen immutable
`diff-tree` and `cat-file` query shapes.

The preregistration commit must have exactly one direct parent, the pinned
v3.5 terminal commit, and exactly one added document. The implementation commit
must have exactly one direct parent, the preregistration commit, and exactly the
twelve strict additions above. Neither may be a merge.

The checker must bind all complete ordinary v3.6 blobs directly. The delta
module and its test must use the same non-circular cross-anchor design as the
reviewed v3.5 proof: the test stores the raw delta-module SHA-256 in
`V36_DELTA_MODULE_SHA256`; the delta module stores the test SHA-256 with only
that anchor's 64 digest characters masked to zero; and canonical syntax plus
every unmasked byte is exact. Same-name/import body changes, comments,
whitespace, anchor syntax, raw framing, baseline pins, replacement counts,
symbol sets, status records, and evidence hashes must all be hostile-tested
and rejected.

The legacy manifest field name `committed_v32_counterpart_comparisons` remains
unchanged solely for manifest-schema and inherited focused-test compatibility.
Its twelve entries must truthfully bind v3.5 counterparts. It does not make
v3.2 or any other predecessor a v3.6 implementation base.

The proof must reject treating `Z` digits as local wall time, applying strict
local gap/fold rejection to a UTC instant, using a fixed offset, accepting or
rounding a nonzero fraction, broadening the timestamp grammar, changing a call
site or unrelated body, adding another fallback/helper/import/global, or
introducing an observed-value special case. The implementation-delta receipt
must be recomputed from committed blobs, never trusted from the worktree.

## Fresh namespace and zero-effect gate

The fresh private checkpoint namespace is
`data/aapl_sec_gemma_lean_evidence_v3_6`; public evidence is under
`e/aapl_sec_gemma_lean_evidence_v3_6`. No v3.6 run artifact, marker, ledger,
journal, receipt, blob, manifest, parse receipt, checkpoint, or public
authority may exist before its defined step. Symlinks, hardlinks, reparse
points, copied predecessor schemas, renamed artifacts, and cross-version
resealing cannot satisfy freshness. The private SEC contact remains readable
only through the inherited ignored local configuration; public evidence may
contain at most its cryptographic fingerprint.

The v3.6 preflight requires a clean branch equal to
`origin/codex/aapl-sec-gemma-lean-evidence-v3-6`, this preregistration as an
ancestor, the exact pushed implementation commit/tree, raw worktree bytes
equal to committed source blobs, no active run, no prior v3.6 probe marker, a
safe writable ignored checkpoint root, and the unchanged private contact. It
must bind its own inherited loaded-source closure and the committed delta
manifest. The later acquisition gate must additionally bind every raw
production-module blob in its unchanged production closure, including the
frozen shared `agent_benchmark/sec_filing_content.py` and
`agent_benchmark/sec_point_in_time.py` blobs from the v3.5 terminal base.

The preflight may make exactly two loopback identity calls in order:
`GET /api/tags`, then `POST /api/show` for `gemma4:12b`. It makes no
`/api/version`, chat, generation, SEC, Yahoo, market, performance, broker, or
trade call. Intent, completion, and publish markers are durable and
non-retryable. Its public receipt must be the only tracked change in its own
commit and must be pushed before acquisition. The v3.6 identity probe may not
be rerun after any durable identity intent, including a probe failure, or
after success. A zero-effect local repository or private-contact gate failure
before any durable identity intent may be corrected and retried.

The production acquisition gate must revalidate that committed receipt,
implementation commit/tree, delta manifest, source inventory,
branch/upstream, clean worktree, a v3.6-only private namespace containing the
authenticated identity-intent, identity-completion, and preflight-publish
markers plus either no acquisition inventory or, on clean resume, only its own
authenticated sealed-prefix state, contact fingerprint, and exact zero-effect
request sequence. It must fail closed without an SEC request on any mismatch.
A v3.6 role may be satisfied only by a fresh v3.6 intent, response, transport
receipt, blob, parse receipt, manifest, and seal.

V3.6 has one fresh namespace and one nonretryable acquisition attempt; this
does not require one process invocation. Clean continuation is allowed only
between fully sealed v3.6 roles after an authenticated clean close. An open
intent, unsealed response, failed role, terminal rejection, or crash debris can
never be adopted, resealed, resent, or resumed. The journal's own
`source_authoritative` field remains false by design; complete authority
requires the full acquisition result, checkpoint, terminal pass, detached
replay, authenticated public source authority, evidence-only preservation
commit, and a separate read-only authentication of that pushed commit.

The production entrypoint remains development-only; intermediate and final
calls reject. The modules expose no Yahoo, Gemma generation,
scientific-feature, portfolio/performance-ledger, broker, or trading path; the
frozen SEC dispatch ledger remains unchanged. A passing parser role is not a
source seal. After a complete development source pass, the generated single
`e/aapl_sec_gemma_lean_evidence_v3_6/development/source-authority-<sha256>.json`,
the redacted `DEVELOPMENT_ACQUISITION.json`, and the comparison update must be
committed and pushed without an implementation or scientific-code change. A
separate read-only gate must authenticate that pushed preservation commit,
allowlisted evidence-only delta, public authority, checkpoint, terminal pass,
and detached replay before inherited development science can open. A
rejection cannot yield authenticated source authority and is preserved instead.
A pre-checkpoint role rejection creates no checkpoint. Any checkpoint or
public artifact without the terminal pass fails authentication and remains
terminal residue. Public evidence may not contain a raw SEC response body,
filing text, or the readable private SEC contact.

## Required offline tests before implementation publication

Focused tests must prove at least:

- strict 14-digit winter and summer wall clocks preserve their exact displayed
  Eastern digits, while a strict spring-gap label and strict autumn-fold label
  reject as nonexistent and ambiguous respectively;
- exact uppercase-`Z` winter and summer instants convert with the correct
  timezone-rule-dependent Eastern offset, including a conversion that crosses
  to the previous Eastern calendar date;
- valid UTC instants whose written digits resemble a local gap or fold remain
  valid, and the two UTC instants mapping to the repeated autumn hour remain
  distinct instants with the correct offsets/fold identities;
- no fraction, one zero fractional digit, and nine zero fractional digits
  accept without rounding or truncation;
- a nonzero digit in an early or late fractional position, lowercase, explicit
  offset, whitespace, missing second, empty or overlong fraction, suffix,
  invalid calendar/time, leap second, or non-string value rejects;
- full synthetic winter and summer complete-submission reconciliation accepts
  a matching UTC Submissions representation and strict Eastern SGML
  representation without changing either raw spelling;
- an exact one-second mismatch rejects; Submissions-only, header-only, and
  both-missing cases keep their inherited behavior; a malformed side cannot
  fall back to the valid side;
- a synthetic date rollover proves the derived `normalized_et`, filing-date
  comparison, availability boundary, original response hash, document-text
  hash, byte offsets, compact serialization, checkpoint, and detached replay;
- every inherited v3.5 family-neutral form, IMS/SEC envelope, raw-byte,
  topology, missing-filename, masking, source-seal, and replay test remains
  byte-mechanically unchanged after namespace derivation;
- every inherited shared-parser fixture returns the same parsed dictionary,
  hashes, document ordering, identities, and offsets, with shared code and
  shared tests unchanged in Git;
- synthetic fixtures do not reproduce the predecessor's accession, filing
  date, literal acceptance values, role/ordinal, response length/hash, header
  hash, document count/topology, filename state, body bytes, filing content, or
  any other unique failed-response identity;
- all v3.5 and earlier blank-company, envelope, stage-plan, cap, transport,
  journal, privacy, crash, timeout, exact-failure, tagged-manifest, and
  detached-replay tests remain valid after mechanical namespace derivation;
- a hostile same-symbol body change, fixed offset, local-digit reinterpretation
  of `Z`, local gap/fold rejection applied to UTC, broad timestamp grammar,
  nonzero-fraction acceptance, observed-value branch, extra helper/import/global,
  non-AST byte change, or cross-anchor mutation fails the committed-blob proof;
- the fresh two-call preflight cannot call version, chat, generation, SEC,
  Yahoo, market, performance, broker, or trade paths;
- a synthetic full-size offline rehearsal still exercises all three unchanged
  stage configurations; and
- the complete v3.5, v3.4, v3.3, v3.2, v3.1, original-v3, and shared-parser
  compatibility suites pass.

Neither tests nor implementation may open or parse the saved v3.5 failed body
or any predecessor private artifact. Synthetic fixtures must establish the
general rule without adapting to further content in that response.

## Hard falsifiers and scientific boundary

Before a complete development source seal, v3.6 rejects on any inherited v3.5
source falsifier plus any of these:

- an original response is rewritten, projected, normalized, trimmed,
  relabelled, or alias-parsed before hashing or source reconciliation;
- a strict 14-digit value does not remain on the inherited gap-and-fold-
  rejecting New York wall-clock path, or an exact uppercase-`Z` value is not
  parsed as a UTC instant and converted through `America/New_York`;
- a fixed offset, local reinterpretation of UTC digits, local gap/fold rejection
  of a UTC instant, rounding, truncation, fallback, lookup, exception table, or
  observed-value branch influences acceptance;
- the UTC grammar admits lowercase, an explicit offset, whitespace, missing or
  extra fields, invalid time, empty/overlong/nonzero fraction, suffix, or other
  spelling outside the one preregistered grammar;
- raw acceptance spellings, response bytes, typed rows, compact evidence,
  checkpoints, hashes, offsets, filing dates, availability inputs, or detached
  replay are rewritten instead of preserving their inherited exact identities;
- malformed, mismatch, one-source-missing, or both-sources-missing behavior is
  weakened, or a malformed side silently falls back to the other side;
- any shared parser or shared parser test byte changes or either path appears
  in the implementation diff;
- any accession, subject, form, filing-date, master-path, Submissions,
  document-topology, filename, availability, privacy, timing, or accounting
  rule is weakened;
- any predecessor private byte, receipt, journal, manifest, parse receipt,
  checkpoint, source authority, or probe marker is read or reused;
- the preregistration is not one document-only child of the exact v3.5
  terminal commit, or the implementation is not one exact twelve-addition
  child of the preregistration;
- any implementation change falls outside the exact twelve-path committed-blob
  and changed-symbol budget;
- request effects are not reported as exact v3.6 counts plus the inherited 367
  family requests, or any request, byte, time, role, or intent cap is exceeded;
- the preflight is rerun after a durable identity intent, a role is retried,
  an unsealed response is adopted, or a terminal attempt is resumed; or
- any unplanned source, model, market, performance, paid-API, broker, or
  trading effect occurs.

If another official source-format or acceptance-time mismatch appears, v3.6
is terminally rejected and preserved. It is not repaired during the attempt.
Any successor requires a separately committed and pushed preregistration based
only on source-format evidence, with science and later stages still closed.

If the development source seal passes, the inherited development science runs
without modification. Confirmation and final data remain closed until their
unchanged gates pass. No historical result can authorize real capital. Any
historically promising survivor must make append-only prospective
paper-trading decisions before outcomes are known; real-money trading remains
outside this goal without fresh explicit user authorization.
