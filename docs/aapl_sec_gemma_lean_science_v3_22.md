# AAPL SEC/Gemma lean science v3.22 preregistration

## Status and plain-language purpose

This document is the complete immutable specification for one fresh,
development-only AAPL-or-cash successor attempt. Creating and pushing this
document performs no qualification, official preflight reservation, source
replay, SEC request, Yahoo request, Ollama request, Gemma generation,
market-value read, prediction, action, return or performance calculation,
confirmation or live-style data open, paid API call, broker action, or
real-money action.

V3.22 exists for one narrow reason. The V3.21 candidate did not test or fail
the trading strategy. Its real isolated runtime proof kept the repository,
dependency, loaded-code, and protected object identities stable, but observed
3,243 protected callables while P321 required 3,242. Two fast differential
probes proved that the difference is exactly one historical-provenance helper,
with no removed callable. V3.22 corrects only that bookkeeping expectation.

In simple terms, the inspected machine has 3,243 named protected tools on the
table, not 3,242. This document records the exact additional tool instead of
pretending it is absent. It does not change what the trading system learns or
how it trades.

V3.22 retains the one shared 4,800-second whole-preflight deadline. It also
retains the deterministic `_strptime` preload, direct publication-recovery
authority, and exact candidate-less zero-effect recovery behavior. It changes
no source, filing cohort, sanitizer, request, model, prompt, market series,
feature, learner, policy, threshold, transaction cost, chronological stage,
scientific gate, or effect budget.

## Immutable base and predecessor evidence

The V3.22 branch is `codex/aapl-sec-gemma-lean-science-v3-22`. Its exact base is
the pushed V3.21 pre-implementation rejection commit
`8ca0d781f266def7ce6adced5d45431617817078`, tree
`0a54c21c6793857f9a976dce62c5cc06bd5e6a28`, whose sole parent is the pushed
P321 preregistration commit
`b00569b0f4b77de354d59d4e32f6829a290059b7`.

At that exact base:

- `docs/aapl_sec_gemma_lean_science_v3_21_rejection.md` is Git blob
  `1763b82a432354cb7628c918f8e3b32227ee5948`, 10,278 bytes, with literal
  SHA-256
  `2fba16e69000851e6a0ad96b1f42d05e1f70e7f99ae7b8abe9f42cd78bcc75dc`;
  and
- `e/APPROACH_COMPARISON.md` is Git blob
  `b58f2a6688a0dfac4ee43685084e6c764cc42bc2`, 40,301 bytes, with literal
  SHA-256
  `430a807d815b3e1a430c87d554c192dc20f2ef702dd1a32a4be3e0d0e706a85e`.

The V3.21 rejection records no `I321`, `F321`, official reservation,
development attempt, prediction, performance comparison, or strategy result.
No such V3.21 object may be invented, copied, resumed, reinterpreted, or used
as V3.22 execution authority.

The document-only V3.22 preregistration commit is named `P322`. It must have the
exact V3.21 rejection commit above as its sole parent, add only this document,
and be pushed and authenticated from the live remote before any V3.22
implementation path is created. Once pushed, P322 is immutable.

## Exact inheritance and sole correction

V3.22 incorporates every normative P321 provision after the exact eight-form
mechanical mapping `V3.21 -> V3.22`, `v3.21 -> v3.22`,
`V3_21 -> V3_22`, `v3_21 -> v3_22`, `V3-21 -> V3-22`,
`v3-21 -> v3-22`, `V321 -> V322`, and `v321 -> v322`, plus the topology
mapping `P321/I321/F321/R321/S321/C321/X321 ->
P322/I322/F322/R322/S322/C322/X322`, except for the explicit replacements in
this document.

The mapping applies to branch, module, class, function, schema, namespace, attempt,
artifact, pending suffix, phase, command, safe-code, comparison-row, and Git
topology identities. It never rewrites quoted historical commits, trees,
blobs, hashes, byte counts, measured timings, V3.19/V3.20/V3.21 facts, or the
V3.21 candidate-source rows below.

Only these provisions are replaced:

1. successor base, branch, preregistration, implementation, namespace,
   attempt, artifact, schema, and topology identities;
2. the six production and six test path names;
3. the protected-callable expectation from 3,242 to exactly 3,243;
4. the runtime callable-delta contract, which requires exactly the one
   historical helper specified below and zero removed V3.19 callable rows;
5. Phase-1 minimum collection and the literal required-node list, by adding
   required node 42 specified below; and
6. the informational planning estimate, from the inherited 3,004.768 seconds
   (about 50 minutes 5 seconds) to 3,004.368 seconds (about 50 minutes 4
   seconds), using the newly measured V3.21 source-chain duration of 1,153.630
   seconds. This estimate replacement does not change the 4,800-second
   deadline or any executable behavior.

The 4,800-second shared deadline is not replaced. There are no other semantic
or process replacements.

## Frozen callable correction

The V3.21 evidence establishes a sealed V3.19 protected baseline of 3,242
callables and a V3.21 candidate count of 3,243. After normalizing only the
successor version identity, the callable graph differential contained exactly
one added row and zero removed rows. The sole added identity was:

```text
owner module: agent_benchmark.sec_gemma_lean_science_v321_contract
qualified name: build_v319_exceptional_rejection_pins
kind: function
```

The exact V3.21 predecessor-evidence row was:

| Field | V3.21 evidence value |
|---|---|
| `object_type` | `builtins.function` |
| `owner_file_sha256` | `bbbbcf979e8919ae5f336fc8c8b3b8612055690616a0693589c926a903f7511f` |
| `owner_slots_sha256` | `9b026ed5295b84d82c3729a9411cfaef560a8a51c99c5bea5339f2f7e01992bd` |
| `annotations_sha256` | `cb259fa450b00d2002bd8b2186c76bb2a71c22f416f1f5a007bd9907b7d89131` |
| `closure_sha256` | `4249bafce15a1d5c3d29ffa5a92474bd3db997369336f9391c103903e6c7d581` |
| `code_sha256` | `1fffe2359bc8b7f84c5df06c2ae180e81618575dabd3677abe961d140da082b5` |
| `defaults_sha256` | `05c9cd6300c6761ac11255b4c1e7746caa2c860ba3096a55d23a494b925b3020` |
| `kwdefaults_sha256` | `05c9cd6300c6761ac11255b4c1e7746caa2c860ba3096a55d23a494b925b3020` |
| `referenced_globals_sha256` | `8fa135fde0fe1e9b6bc977c46a9f8facb1a2ca9c4e29ed08815b5edf45fc5065` |
| `descriptor_members_sha256` | `null` |
| `owning_binary_sha256` | `null` |

Those hashes authenticate the V3.21 candidate evidence only. Version mapping,
base pins, and final production bytes will change the V3.22 owner file, code,
referenced-global, and owner-slot material. I322 must therefore derive and
freeze its own exact helper row from its final V3.22 production bytes. It must
not copy any V3.21 row hash and claim it is a V3.22 value. The normalized
identity, exactly-one-added cardinality, and zero-removed cardinality are the
immutable cross-version requirements.

The helper `build_v319_exceptional_rejection_pins` must occur exactly once in
the V3.22 protected callable graph. It remains the one historical-provenance
helper already present in the V3.21 candidate. No new production function,
class, lambda, partial, adapter, callable instance, or other callable may be
introduced by V3.22.

In particular, V3.22 must not add `build_v321_rejection_pins` or any equivalent
helper. Doing so would raise the total to 3,244 callables and violate this
preregistration. V3.21 rejection evidence must be
bound through updated `BASE_*` values plus non-callable constants and inlined
data inside existing manifest or source-authority builders only. Existing
builders may be mechanically updated; no new callable may be created to hold
that evidence.

## Correction of the earlier V3.20 attribution

The V3.20 rejection said its measured 3,243-versus-3,242 result was exactly the
intermediate recovery wrapper
`build_production_development_dependencies.<locals>.authenticate_recovery`.
The later evidence proves that attribution was incomplete and wrong as an
explanation of the protected count difference. The V3.21 runner removed the
wrapper and used the required direct binding, yet the wrapper-free manifest
still contained 3,243 callables. The exact differential identified
`build_v319_exceptional_rejection_pins` as the sole added protected row.

V3.22 preserves both facts honestly: the wrapper remains forbidden, and the
historical-pins helper is explicitly counted once. The old rejection document
remains immutable historical evidence; V3.21's pushed rejection is the
evidence-backed correction.

## Frozen timing and useful runtime estimate

The official V3.22 preflight retains one absolute monotonic deadline of exactly
`80 * 60 == 4,800` seconds. The deadline is allocated only after the once-only
reservation is consumed and is shared unchanged by collection, execution, all
three qualification phases, runtime-authority construction, authenticated
source/projection/request work, privacy checks, and durable finalization.

It cannot be restarted, retried, split into per-phase clocks, lengthened
dynamically, replaced by wall-clock time, or extended after a slow operation.
The outer official child wait and termination timeout is bounded by the
positive duration remaining on that deadline. Any inherited narrower local
subprocess or cleanup bound remains only a local upper bound and cannot
authorize continued work or a passing artifact at or after the shared
deadline. Reaching or crossing the deadline rejects the attempt and cannot
create a passing artifact.

The latest honest planning estimate uses the sealed F319 qualification total
of 697.108 seconds and the newly measured 1,153.63-second real source-chain
node twice:

```text
697.108 + 2 * 1,153.63 = 3,004.368 seconds
```

That is about 50 minutes 4 seconds of expected work. The hard ceiling remains
80 minutes. The estimate is not a promise, timeout extension, or second clock;
it is a simple progress expectation for the user. Before consuming the
reservation, the operator must print approximately 50 minutes expected,
80 minutes hard maximum, the single-attempt fact, and the current
clean-state/zero-effect result. During execution, useful logs may report the
completed phase, monotonic elapsed time, and time remaining under the same
4,800-second deadline without exposing private source.

## Hash-preserved V3.21 candidate inputs

The exact twelve uncommitted V3.21 candidates inspected before this
preregistration total 1,898,729 bytes. Their canonical compact sorted-row JSON
manifest is 2,002 bytes with literal SHA-256
`fd49940c63bffd350aeb0ee6db5aa8934b34ac0c973cc43f7e12c3da88763a6b`.
Each canonical row has exactly `byte_count`, `literal_sha256`, and `path`:

| V3.21 candidate path | Bytes | Literal SHA-256 |
|---|---:|---|
| `agent_benchmark/sec_gemma_lean_science_v321_bridge.py` | 93,155 | `abfd8e50bf65248b392c2ccd9e709777c3e2fe02620b7c416f5134b35dbacc36` |
| `agent_benchmark/sec_gemma_lean_science_v321_contract.py` | 169,063 | `bbbbcf979e8919ae5f336fc8c8b3b8612055690616a0693589c926a903f7511f` |
| `agent_benchmark/sec_gemma_lean_science_v321_journal.py` | 21,869 | `7baa95c74522126b89cc05296f776b46e078aa458e9628b0de7d5b93f73d5014` |
| `agent_benchmark/sec_gemma_lean_science_v321_preflight.py` | 557,764 | `71540d2e7a1d8ba299ee76bc5ca2983316f126be20de004ff3173a02364b9d5e` |
| `agent_benchmark/sec_gemma_lean_science_v321_runner.py` | 227,239 | `d08daf5d7007d4181a8cc262f6aff434b917f3e8a1fba15c62d7f095b49af033` |
| `agent_benchmark/sec_gemma_lean_science_v321_store.py` | 192,331 | `ea763731c385fc5ad14f4d0d8534b2ae92e12f5784ed18930749729ab7205578` |
| `tests/test_sec_gemma_lean_science_v321_bridge.py` | 69,745 | `98bde54c8d51c797027729698d05f41cc9f3db2e510e9bf4a503e744bd1b8a41` |
| `tests/test_sec_gemma_lean_science_v321_contract.py` | 71,856 | `bfad8db04fcd97c60b04fd3e6af30ac80f5491cf8387c7507f778c89f88d5537` |
| `tests/test_sec_gemma_lean_science_v321_journal.py` | 8,463 | `fdaff08cc04621aa160c8651542be5649ff32d1536fdbc32d64dfdc0cf0287a8` |
| `tests/test_sec_gemma_lean_science_v321_preflight.py` | 225,684 | `e4b31e7eae19cd71dbd9010f84238039e74ce0dbd405c0933f5a987da2734bc5` |
| `tests/test_sec_gemma_lean_science_v321_runner.py` | 198,964 | `5171e9817fe2781f4a586393bf1f9fb0c87f44bbb27a2fe38e0088e0acffa437` |
| `tests/test_sec_gemma_lean_science_v321_store.py` | 62,596 | `649d88fa1145e865c6f38d0ee1d7eab0829c89ea99c66417bd271a733fb836a3` |

These hashes authenticate source derivation only. They are not an `I321`
commit, V3.21 execution authority, official receipt, runtime namespace, or
strategy result. P322 does not add, delete, rename, stage, or commit any of the
twelve candidates.

## Exact V3.21-to-V3.22 path mapping

After P322 is pushed and authenticated, I322 may derive exactly these twelve
successor paths from the hash-preserved candidates:

| Preserved source | New V3.22 path |
|---|---|
| `agent_benchmark/sec_gemma_lean_science_v321_contract.py` | `agent_benchmark/sec_gemma_lean_science_v322_contract.py` |
| `agent_benchmark/sec_gemma_lean_science_v321_bridge.py` | `agent_benchmark/sec_gemma_lean_science_v322_bridge.py` |
| `agent_benchmark/sec_gemma_lean_science_v321_journal.py` | `agent_benchmark/sec_gemma_lean_science_v322_journal.py` |
| `agent_benchmark/sec_gemma_lean_science_v321_store.py` | `agent_benchmark/sec_gemma_lean_science_v322_store.py` |
| `agent_benchmark/sec_gemma_lean_science_v321_preflight.py` | `agent_benchmark/sec_gemma_lean_science_v322_preflight.py` |
| `agent_benchmark/sec_gemma_lean_science_v321_runner.py` | `agent_benchmark/sec_gemma_lean_science_v322_runner.py` |
| `tests/test_sec_gemma_lean_science_v321_contract.py` | `tests/test_sec_gemma_lean_science_v322_contract.py` |
| `tests/test_sec_gemma_lean_science_v321_bridge.py` | `tests/test_sec_gemma_lean_science_v322_bridge.py` |
| `tests/test_sec_gemma_lean_science_v321_journal.py` | `tests/test_sec_gemma_lean_science_v322_journal.py` |
| `tests/test_sec_gemma_lean_science_v321_store.py` | `tests/test_sec_gemma_lean_science_v322_store.py` |
| `tests/test_sec_gemma_lean_science_v321_preflight.py` | `tests/test_sec_gemma_lean_science_v322_preflight.py` |
| `tests/test_sec_gemma_lean_science_v321_runner.py` | `tests/test_sec_gemma_lean_science_v322_runner.py` |

The derivation performs only the version/topology/base mapping, the exact
3,243-callable correction, the V3.21-rejection evidence binding described
above, and the required test changes in this document. All final V3.22 byte
counts and hashes must be regenerated; V3.21 hashes may not be relabeled as
V3.22 hashes.

## Implementation topology and callable-safe provenance binding

`I322` has P322 as its sole parent and adds exactly the twelve V3.22 paths in
the right-hand column above. No tracked predecessor path may change. The six
V3.22 production modules may not import or execute V3.21, V3.20, V3.19, older
production/test modules, or historical strategy entry points. The inherited
37-module shared closure, package initializer, standard library, and exact
qualified third-party runtime remain the only allowed imports outside the six
successor production modules.

The V3.22 contract must update its non-callable base and rejection constants to
the exact V3.21 rejection identities in this document. Any V3.21 rejection
dictionary must be inlined inside an already existing manifest or
source-authority builder. It must not be returned by a new helper. Production
AST and loaded-graph audits must prove that the mapped six-module production
set adds no function, class, or other callable beyond the one inherited
`build_v319_exceptional_rejection_pins` row.

The production recovery dependency remains this exact direct object binding:

```text
authenticate_publication_recovery = load_publication_recovery_authority
```

It must be object-identical to the imported dedicated recovery authority. It
must not be replaced by a nested callable, lambda, partial, adapter, broad
execution-authentication call, exception fallback, or any semantically
equivalent wrapper. The deterministic `_strptime` preload and exact
candidate-less zero-effect recovery predicate remain mechanically mapped
behavior except for successor identities.

After the final six production files stop changing, I322 must derive the exact
V3.22 helper row hashes from those bytes and freeze them in the V3.22 tests.
Changing a production byte after that derivation invalidates those hashes and
requires regeneration before commit. I322 must then derive and record its own
exact twelve-path byte counts, Git blob identities, literal SHA-256 values,
aggregate manifest, repository tree, protected source/callable manifests, and
runtime authority. All must be re-authenticated from the pushed I322 bytes.

## Required direct tests and qualification

Every V3.21 candidate test remains mandatory after mechanical V3.22 mapping.
The existing long runtime node must expect exactly 3,243 protected callables
and continue to require exact repository, dependency, loaded-code, and
process-object equality before and after real preserved-source authentication,
streaming projection, request commitments, and model-plan construction.

Phase 1 runs every collected node in the six V3.22 test files. It must collect
at least 343 case-sensitive unique nodes with zero duplicates, execute the
exact ordered collected list once, and pass every node with collection and
execution parity and zero skip, xfail, or xpass.

The exact 41 P321 required literal node IDs remain required after mechanical
V3.22 mapping. One new literal, unparametrized required node is added, for
exactly 42 required IDs:

42. `tests/test_sec_gemma_lean_science_v322_preflight.py::test_runtime_callable_delta_is_exact_one_provenance_helper_and_no_recovery_wrapper`

Required node 42 must use real V3.22 production objects and prove all of the
following:

- the protected callable count is exactly 3,243;
- the normalized differential against sealed F319/V3.19 contains exactly one
  added callable row and zero removed callable rows;
- the added row is the V3.22 contract-owned function
  `build_v319_exceptional_rejection_pins` and occurs exactly once;
- every V3.22-specific row hash equals the value derived and frozen after the
  final V3.22 production bytes were locked, not a copied V3.21 value;
- no `build_v321_rejection_pins` function or equivalent new callable exists;
- no recovery wrapper, lambda, partial, adapter, broad execution-auth fallback,
  or equivalent callable exists;
- `authenticate_publication_recovery` is object-identical to
  `load_publication_recovery_authority`; and
- the six production modules contain no other added callable after normalized
  comparison with the mapped V3.21 candidate inventory.

For the cross-version comparison, each callable is projected to an object with
exactly `kind`, `object_type`, `owner_module`, and `qualified_name`; keys are
sorted, rows retain canonical manifest order, compact UTF-8 JSON uses `,` and
`:` separators with no trailing newline, and successor version identities in
both `owner_module` and `qualified_name` are normalized to their V3.19 form.
After the sole helper row is removed, the
projection must contain exactly 3,242 rows, encode to exactly 541,613 bytes,
and have SHA-256
`8324901df84c3cfc6fb03e790f57bcd80baaffadf70f974a68c28961a0f913b7`.
This public aggregate identity permits the test without treating mutable
private V3.19 files as authority. Callable comparison remains a multiset
comparison because distinct protected objects can legitimately share a name.

The inherited deadline node remains required and must prove the unchanged
single 4,800-second deadline. The `_strptime`, direct recovery,
candidate-less recovery, real import-closure, privacy, and source/runtime nodes
all remain required.

Phase 2 remains the exact 14 ordered shared selectors: exactly 655 ordered
nodes, 655 case-sensitive unique IDs, zero duplicates, and ordered node-list
SHA-256
`1de8cc183af68c089aa5616f5e51405abda34382ff15e73123f5cd3c0dfea83c`.
Phase 3 remains the one exact Requests-identity sentinel. Controlled
environment, exact collection/execution parity, zero skip/xfail/xpass policy,
subprocess completion records, private/public validation, and once-only
reservation are unchanged.

Local work before I322 and before the official reservation is diagnostic only.
It may not create an official namespace, public artifact, launch proof,
attempt identity, market/model response, or performance evidence, and it may
not be described as an official pass.

## Mandatory local, pre-push, and live-remote proof

Before I322 is committed, before it is pushed, and again immediately before
the official reservation, the exact callable-safe source derivation and all
tests that can authenticate the relevant bytes must be checked. The inherited
real-HEAD import-closure node is intentionally commit-bound: it is omitted
before I322 exists, then must pass against exact local I322 before push and
against the authenticated live-remote I322 state before reservation. A passing
local candidate does not authenticate different committed or live-remote
bytes.

The inherited redacted real-source proof must authenticate the preserved V3.8
source and report exactly:

- 75 canonical requests;
- seven requests with direction removals;
- eight removed sentences, partitioned as four current-period and four
  prior-period sentences;
- zero empty required partitions;
- zero V3.22 contract failures;
- zero inherited-production failures; and
- zero external effects.

The runtime proof must preload `_strptime` and use the real repository,
dependency, owner-graph, and loaded-code builders. It must report exactly:

- 44 repository module rows;
- 366 dependency module-file rows;
- 3,243 protected callable rows;
- exactly one normalized added helper row and zero removed rows relative to
  sealed F319/V3.19;
- the exact once-only `build_v319_exceptional_rejection_pins` identity with
  V3.22-derived row hashes;
- no new callable and no recovery wrapper; and
- exact repository, dependency, loaded-code, and protected process-object
  equality before and after the complete real source/projection/request/plan
  chain.

The local pre-commit proof derives the final production-row hashes. The
pre-push proof authenticates the exact proposed I322 tree. The live proof must
rebuild the same facts from the exact pushed I322 commit and remote bytes. Any
count, delta, hash, identity, or before/after mismatch blocks I322 or blocks the
official reservation. Only redacted aggregates and hashes may be printed;
private paths, contact data, filing bodies, request bodies, offsets, and source
content remain private.

Immediately before reservation, all effect counters must still be zero; no
Yahoo, Ollama, Gemma, SEC transport, or market/model open may have occurred;
the V3.22 private namespace and public artifact must be absent; HEAD must be the
exact pushed I322 commit and tree; local and remote must agree; and tracked,
staged, and untracked state must be exactly clean. The official command and
environment must match the inherited scientific bootstrap exactly.

If the machine cannot safely provide the full 4,800-second window, a mandatory
precheck fails, or the approximately 50-minute estimate is no longer honest,
the reservation must not be consumed. The issue must instead be preserved
under the inherited pre-implementation rejection topology.

## One official attempt and authority isolation

V3.22 authorizes exactly one official preflight reservation. A consumed
reservation can never be retried, even after interruption, restart, code
change, branch change, timeout, or apparent environmental repair. A failed
official preflight is preserved through mapped X322 topology; it never becomes
a second attempt.

The official V3.22 preflight creates a fresh V3.22 namespace, identity,
one-shot reservation, runtime authority, and public artifact. It must not read,
copy, manufacture, or accept a V3.21 private directory, launch proof, recovery
authority, attempt identity, journal, candidate, terminal receipt, result,
pause, continuation, or public artifact. The V3.21 candidate hashes authenticate
only the source derivation described above; they authenticate no execution
state.

Publication recovery is restricted to a V3.22 store created by the exact
pushed F322 authority and the exact mechanically inherited recovery
predicates. It cannot create a new development attempt, turn a V3.21 rejection
into V3.22 state, add a callable, or introduce an external effect.

## Unchanged science, source plan, and chronological gates

The preserved source and filing cohort, direction sanitizer and shared final
predicate, seven-request/eight-sentence aggregate, 75 canonical requests,
fixed local `gemma4:12b` model, prompt, context, output schema, Yahoo series,
features, learner, AAPL-or-cash policy, thresholds, transaction costs,
development gates, confirmation closure, live-style closure, runtime-v2
identities, Requests sentinel, and terminal construction remain unchanged from
P321 after version mapping.

Development remains 2000-2018, confirmation remains 2019-2023, and live-style
evaluation remains 2024 onward. Confirmation and live-style data remain closed
unless all 22 inherited development gates pass and the separately pushed
result gate authenticates the exact result. Buy-and-hold remains evaluated at
both 5 bps and 10 bps.

The system remains long-only AAPL or cash. No shorting, leverage, borrowing,
negative cash, paid API, broker action, or real-money action is authorized.
The SEC source remains the authenticated preserved local source under its
unchanged privacy and identity rules. The model remains local and frozen. No
future information may cross a chronological boundary.

Changing a callable count does not make the strategy more likely to win,
change a return, relax a gate, select a better period, reveal later data, or
authorize confirmation. A passing preflight is only permission to begin the
development stage. It is not evidence that the system beats buy-and-hold.

## Terminal Git topology and honest outcomes

P322 adds only this preregistration. I322 has P322 as sole parent and adds only
the twelve V3.22 production/test paths listed above. Once-only F322 has I322 as
sole parent and adds only its validated public preflight artifact. R322 adds
only an authenticated terminal result and the comparison-table change. S322
adds only a pause artifact. C322 adds only a continuation document. A consumed
preflight failure uses mapped X322 topology. Every failure, pause, and
successful result must be committed and pushed as immutable evidence before a
successor may rely on it.

If official preflight misses the 4,800-second deadline or any frozen check,
V3.22 is permanently rejected and no development or later stage opens. If
preflight passes, development may run once under the inherited rules. If any
development gate fails, V3.22 is rejected and confirmation remains closed. If
development passes, the pushed-result gate must authenticate exact
private/public evidence before confirmation can be considered. A later success
claim requires the frozen buy-and-hold comparisons and every chronological
gate; absence of a result is never a win.

Nothing in this preregistration authorizes real-money trading.
