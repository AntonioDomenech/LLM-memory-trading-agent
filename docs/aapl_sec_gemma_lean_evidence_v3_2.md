# AAPL SEC/Gemma lean evidence v3.2

## Status and immutable predecessor

This is a new, separately preregistered source-authority successor on
`codex/aapl-sec-gemma-lean-evidence-v3-2`. This document must be committed and
pushed by itself before any v3.2 implementation commit, programmatic SEC
request, Yahoo value, Gemma generation, performance calculation, confirmation
source, or final source is opened.

V3.1 is terminally rejected and preserved at commit
`faf71b8cd897afb8094684089f167b3832c4bcd8`, tree
`2a75c4ff2c2bcb03ad5060ab4ba47a66d67ca477`. Its redacted rejection artifact is
`e/aapl_sec_gemma_lean_evidence_v3_1/DEVELOPMENT_ACQUISITION.json`, with file
SHA-256 `9c6ed31a0e3cfb2979e9445837449c1aebe45cf8738e68ea8eafac50b0510895`
and internal acquisition SHA-256
`f46331d0ba6c16128e88b0f084f6b430b73da4d34050517fcaaf9813d99f0984`.
V3.2 never repairs, resumes, or reclassifies that attempt.

The v3.1 scientific preregistration remains pinned at commit
`fe38fce300e858eeddb6894a540b8e52830d9287`, document blob
`db5bf34d80a18affded60084c4d98b351dccfe1b`, and document SHA-256
`c066dc87cd30b15cfa517572f97c53fc028a8eb3fd7ed65913c122883512febd`.
The v3.1 implementation commit is
`281aec0f2e1790ded4a3c2ead34301ce57adf392`, tree
`97dcd2ae1959dc7573d53d605376522ab17fb8e2`. Its zero-effect preflight is
preserved at evidence commit `ed57ff8eb834736ac1b70e91261d5257e72d1920`,
tree `2949efb649899e20d00395c54686d3b71a7734b6`, with preflight SHA-256
`44eb679d129a9aa552b83cbc123362c6a656c29a8859258ccfc8356af0c98703`.
The original scientific parent remains
`efbc481c57e480d48303763163676e64e87df49d`.

## Evidence already seen and evidence still closed

V3 made four fully journaled official SEC catalogue requests before its own
preserved source-integrity rejection. Those responses are previously seen
source-only evidence and remain unusable by v3.2. V3.1 then made exactly two
official SEC requests. Both returned HTTP 200 and were sealed:

1. the main `CIK0000320193.json` Submissions response; and
2. `CIK0000320193-submissions-001.json`.

Those two response bodies, their typed rows, and their failure are now
previously observed source-only evidence. They are not unseen v3.2 evidence and
cannot justify a model, prompt, feature, threshold, corpus, learner, or
performance change. V3.2 will reacquire every role into a fresh namespace and
will identify the two Submissions roles as repeated source evidence. No v3 or
v3.1 raw byte, receipt, journal event, or checkpoint can satisfy a v3.2 role.

The observed v3.1 snapshot contained exactly one exact non-amended Apple target
before 1994-Q3: accession `0000320193-94-000002`, form `10-Q`, filed
`1994-01-26`. The frozen v3.1 boundary therefore returned
`master_boundary_incomplete` before any master request. This filing is not
excluded, waived, rewritten, or given a special URL in v3.2.

No 1994-Q1 or 1994-Q2 master body or availability result has been opened. The
controlled v3.1 acquisition opened no complete-submission body, Yahoo value,
market value, Gemma generation, performance calculation, confirmation source,
final source, paid API, broker, or trade. Before the v3.1 preregistration, the
public filing page and raw complete-submission body for accession
`0000912057-00-023442` were inspected only to understand the known legacy
missing-`FILENAME` format. That design-only body is previously seen,
inadmissible, and unusable by v3.2. No admissible filing-text, model, market, or
performance evidence has been opened, so the scientific contribution of the
filing text and trading approach remains unevaluated.

## Frozen hypothesis and unchanged science

The scientific question remains literal and unchanged:

> Can a fixed local Gemma reader extract point-in-time deterioration evidence
> from Apple 10-K and 10-Q filings that helps a chronological long-or-cash
> learner avoid enough harmful AAPL exposure to add after-cost value over the
> frozen exhaustion baseline and a no-filing-meaning control?

V3.2 inherits every scientific rule from the pinned v3.1 preregistration:

- model identity, prompt, schema, seed, temperature, context, and output caps;
- the twelve filing values, market features, and missingness flags;
- learner, fitting, threshold, interval, non-overlap, and next-open rules;
- inherited exhaustion baseline, semantic control, and no-meaning control;
- development, confirmation, final, and frozen-learning gates;
- 5-bps and 10-bps same-ledger AAPL buy-and-hold comparisons; and
- AAPL-or-cash only, no shorting, no leverage, no borrowing, no negative cash,
  no paid API, no real-money action, and no future leakage.

`H`, `I`, `P`, `U`, `D`, their caps, the 95% acceptance requirement, admitted
development/intermediate/final counts, chronological windows, stage ordering,
and every performance threshold remain unchanged. No row deletion, single-row
exception, prompt rewrite, feature change, class rebalance, model swap,
threshold search, alternate horizon, or performance-informed repair is
allowed.

## The only source-authority repair

The quarterly-master coverage start moves monotonically from 1994-Q3 to
1994-Q1. Coverage is continuous: both Q1 and Q2 are mandatory even though the
previously observed Submissions snapshot has no exact Q2 target. The full
official URL sequence starts with:

- `https://www.sec.gov/Archives/edgar/full-index/1994/QTR1/master.gz`; and
- `https://www.sec.gov/Archives/edgar/full-index/1994/QTR2/master.gz`.

There is no alternate filename, daily-index fallback, directory probe,
redirect, cache, retry, or Submissions-only waiver. A missing, non-200,
malformed, ambiguous, truncated, concatenated, or mismatched Q1/Q2 master
terminally rejects v3.2. If official quarterly-master authority does not exist
for either quarter, this successor fails; it does not invent weaker authority.

Every exact non-amended Apple 10-K/10-Q Submissions row filed on or after
`1994-01-01` and at or before the fixed stage cutoff must exact-set reconcile to
one master row on accession, Apple subject CIK, form, filed date, and canonical
`edgar/data/320193/<accession>.txt` path. Any target filed before 1994-Q1 still
rejects as `master_boundary_incomplete`. Any missing, unexpected, substituted,
duplicated, or conflicting master row rejects.

The observed 1994-01-26 filing is treated exactly like every other Q1 member:
it must match the Q1 master, use only that master's exact complete-submission
path, and pass the unchanged complete-header, primary-document, acceptance,
change-date, availability, and text rules. Its preliminary Submissions
acceptance cannot exclude it. A missing Submissions change date cannot prove
that its unseen authenticated header lacks a later change date.

## Frozen stage configurations and caps

Only `Q` and the mechanical request/byte totals change:

| Stage | Availability window | Master coverage | `Q` | `I` cap | `P` cap | Admitted `D` | Maximum requests |
|---|---|---:|---:|---:|---:|---:|---:|
| Development | 2000-01-01 through 2018-12-31 | 1994 Q1 through 2018 Q4 | 100 | 128 | 128 | 72-80 | 245 |
| Intermediate | 2019-01-01 through 2023-12-31 | 1994 Q1 through 2023 Q4 | 120 | 160 | 24 | 19-20 | 161 |
| Final | 2024-01-01 through 2026-07-09 | 1994 Q1 through 2026 Q3, filtered at the cutoff | 131 | 176 | 16 | at most 12 plus every inherited completed-year rule | 164 |

A passing stage still has exactly `1 + H + Q + P` successful official SEC
requests. `H <= 16`; lifetime intents remain capped at 256 per stage. The
individual limits remain 32 MiB for main Submissions, 64 MiB per historical
file, 16 MiB compressed and 128 MiB decompressed per master, and 128 MiB per
complete submission.

The exact aggregate successful-response cap becomes 18.59375 GiB
(`19,964,887,040` bytes), adding only two 16-MiB master bodies. The exact
aggregate decompressed-master cap becomes 16.375 GiB (`17,582,522,368` bytes),
the final-stage sum of 131 128-MiB master limits. The conservative lifetime
received-byte cap remains 20 GiB (`21,474,836,480` bytes). All `L + 1`
reservations, exact effect accounting, TLS, privacy, timeout, pause/resume,
one-intent, no-retry, and crash-open rules remain unchanged.

## Exact failure-code and manifest-integrity repairs

Two evidence-integrity defects observed only after v3.1 stopped must be fixed
before v3.2 can dispatch:

1. The exact diagnostic-code allowlist is the singleton
   `{master_boundary_incomplete}`. A caught source error preserves its complete
   message only when it equals that one literal; every other message, including
   another lowercase underscore token, sentence, exception detail, URL, header,
   body content, or contact fragment, becomes `source_rejected`. The preserved
   diagnostic cannot affect dispatch, retry, resume, inclusion, ordering, or any
   scientific gate. Hostile tests must show that secret-bearing, arbitrary, and
   merely code-shaped errors are never persisted.
2. Compact role manifests continue to store body identity as exactly
   `sha256:<64 lowercase hex>`. The acquisition inventory loader must require
   that tagged representation at the manifest boundary, compare its bare
   suffix to the exact blob bytes, and reject bare, double-tagged, uppercase,
   malformed, or mismatched alternatives. Other bare-hash fields remain bare.

The detached inventory loader must successfully authenticate every sealed role
after both a passing terminal and a clean terminal rejection. These repairs do
not authorize adoption of crash debris, an open role, a failed response, or an
unsealed blob.

## Canonical v3.2 sequence

After the Git gates below pass, development runs in this order:

1. reacquire the main Apple Submissions JSON once;
2. reacquire every referenced historical Submissions file once in canonical
   filename order;
3. build rejection-only pre-master `I` and `P` upper bounds;
4. fetch every master from 1994-Q1 through 2018-Q4, with Q1 and Q2 first;
5. exact-set reconcile the complete Submissions/master candidate set;
6. fetch every required complete submission in `(filing_date, accession)`
   order from the exact master filename;
7. finalize the complete development source seal and exact detached replay;
8. only then allow inherited development Yahoo values, fixed Gemma requests,
   features, labels, ledgers, or performance calculations.

Intermediate remains forbidden unless development passes its unchanged
scientific gate. Final remains forbidden unless intermediate passes. V3.2
reacquires its own response-time evidence; v3.1 bytes are diagnostic predecessor
evidence only.

## Implementation and zero-effect gate

The mandatory sequence is:

1. commit and push this document alone;
2. add the exact v3.2 implementation and focused offline tests;
3. commit and push that implementation;
4. run a fresh zero-source-effect v3.2 preflight;
5. commit and push the preflight receipt alone; and
6. only then make the first v3.2 SEC request.

The implementation commit may add exactly these twelve files and may modify no
inherited tracked file:

- `agent_benchmark/sec_gemma_lean_v32_acquisition.py`;
- `agent_benchmark/sec_gemma_lean_v32_delta.py`;
- `agent_benchmark/sec_gemma_lean_v32_journal.py`;
- `agent_benchmark/sec_gemma_lean_v32_preflight.py`;
- `agent_benchmark/sec_gemma_lean_v32_source.py`;
- `agent_benchmark/sec_gemma_lean_v32_transport.py`;
- `tests/test_sec_gemma_lean_v32_acquisition.py`;
- `tests/test_sec_gemma_lean_v32_delta.py`;
- `tests/test_sec_gemma_lean_v32_journal.py`;
- `tests/test_sec_gemma_lean_v32_preflight.py`;
- `tests/test_sec_gemma_lean_v32_source.py`; and
- `tests/test_sec_gemma_lean_v32_transport.py`.

Each v3.2 production module must be mechanically derived from its committed
v3.1 counterpart. The v3.2 delta checker must compare committed blobs and prove
that differences are limited to:

- v3.2 module imports, schema strings, namespaces, branch and Git pins;
- the 1994-Q1 start, three `Q` counts, request totals, and two aggregate caps;
- exact safe source-error propagation at catch boundaries;
- exact tagged manifest-body hash loading; and
- the fresh v3.2 preflight/acquisition closure and tests.

No inherited v3.1 or shared scientific file may change. A new-file static
inventory must bind every symbol and import. The implementation delta receipt
must be recomputed from committed blobs, not the worktree.

The fresh checkpoint namespace is
`data/aapl_sec_gemma_lean_evidence_v3_2`; public evidence is under
`e/aapl_sec_gemma_lean_evidence_v3_2`. The private SEC contact remains readable
only from the ignored `data/local_config.json` and public evidence may contain
at most its cryptographic fingerprint.

The v3.2 preflight requires a clean branch equal to
`origin/codex/aapl-sec-gemma-lean-evidence-v3-2`, this preregistration as an
ancestor, the exact pushed implementation commit/tree, raw worktree bytes equal
to committed source blobs, no active run, no prior v3.2 probe marker, a writable
ignored checkpoint, and the unchanged private contact. It may make exactly two
loopback identity calls in order: `GET /api/tags`, then `POST /api/show` for
`gemma4:12b`. It makes no `/api/version`, chat, generation, SEC, Yahoo, market,
performance, broker, or trade call. Intent, completion, and publish markers are
durable and non-retryable. Its public receipt is committed and pushed alone.

## Required offline tests before implementation publication

Focused tests must prove at least:

- all three stages start at 1994-Q1 with exact `Q` and maximum-request counts;
- transport accepts canonical 1994-Q1/Q2 master URLs and rejects 1993-Q4,
  malformed, alternate, redirected, or out-of-range URLs;
- the master role plan begins Q1 then Q2 and cannot skip or reorder either;
- a target on 1994-01-01 is included and a target before it rejects;
- the observed 1994-01-26 fixture is handled only by the general exact-set rule;
- a missing Q1/Q2 response or a Q1 target mismatch terminally rejects;
- `master_boundary_incomplete` is journaled exactly while arbitrary or
  secret-bearing source exceptions become `source_rejected`;
- tagged manifest body hashes load and bind the exact blob, while bare,
  malformed, uppercase, double-tagged, or mismatched values reject;
- terminal rejection remains detached-replayable and non-resumable;
- exact request, byte, `L + 1`, TLS, privacy, crash, and timing controls remain;
- the fresh two-call preflight cannot call version, chat, generation, SEC,
  Yahoo, market, performance, broker, or trade paths; and
- a synthetic full-size offline rehearsal exercises all three configurations.

## Hard falsifiers and scientific boundary

Before a complete development source seal, v3.2 rejects on any inherited v3.1
source falsifier plus any of these:

- unavailable or invalid 1994-Q1 or Q2 quarterly-master authority;
- any pre-1994-Q1 exact target;
- any omission, conflict, or substitution involving the observed Q1 filing;
- any implementation change outside the twelve-file and symbol budget;
- failure to preserve the exact safe boundary code or authenticate tagged role
  manifests; or
- any unplanned source, model, market, performance, or trading effect.

If Q1/Q2 official master authority is unavailable or cannot reconcile, v3.2 is
terminally rejected. A later alternative would require a separately
preregistered, independent, official completeness authority for early 1994;
Submissions alone is insufficient under this exact-universe design.

If the development source seal passes, the inherited development science runs
without modification. Confirmation and final data remain closed until their
unchanged gates pass. No historical result can authorize real capital. Any
historically promising survivor must make append-only prospective paper-trading
decisions before outcomes are known; real-money trading remains outside this
goal without fresh explicit user authorization.
